# 论文深度讲解.md — 统一合并规范（三终端共用）

> 用途：把每个论文目录下的 `*_标准化总结.md` + `模型梳理.md` + `论文深度讲解.md`
> 三份文档**合并为唯一一份** `论文深度讲解.md`，去重、补全、校正。
> 参考标准：[芯片流程.md](芯片流程.md)（17 阶段，编号 ①-⑰）

---

## 一、任务定义

对分配到的每个论文目录，执行：

1. **读原文**：目录下的 `*.txt`（已由 pdftotext -layout 提取，是最权威的事实来源）。
   若 txt 存在乱码/表格错位，用 `pdftotext -f N -l M -layout` 重提该页，或 `pdftoppm -f N -l N -r 150 -png` 截图后用 Read 看图。
2. **读三份已有文档**，抽取各自的独有信息：
   - `*_标准化总结.md` → 论文元信息、作者机构、实验数据表、评估指标、局限性
   - `模型梳理.md` → 代码仓库地址、本地复现状态、下载链接
   - `论文深度讲解.md` → 已有的流程图、公式、阶段映射
   - `*论文与代码复现详解.md`（若有）→ **只提炼要点**（论文↔代码差异、复现门槛、关键坑），**不要全文搬运**，原文件保留不动
3. **核对**：三份文档之间凡有数字/结论冲突，一律**以 txt 原文为准**，并在正文注明修正。
4. **写出唯一的** `论文深度讲解.md`（覆盖原文件）。
5. **删除** 该目录下的 `*_标准化总结.md` 和 `模型梳理.md`（内容已 100% 并入后才删）。

---

## 二、输出文件名

| 情况 | 文件名 |
|---|---|
| 目录下只有 1 篇论文 | `论文深度讲解.md` |
| 目录下有多篇论文 | `论文深度讲解_<论文简称>.md`，每篇一个文件 |

多篇论文的目录，**额外在最后一个文件末尾加一节「与本目录另一篇的对比」**，或单独一份
`论文深度讲解_对比.md`（仅当对比内容超过 3KB 时才单独成文）。

已有的 `论文深度讲解-MultiAgent.md`、`Revisiting 论文深度讲解.md`、
`论文深度讲解_2212.11140_Benchmark版.md` 等**非规范命名的旧文件，重命名为规范名**（用 `git mv` 或 `mv`），
内容合并后删除多余的空壳 `论文深度讲解.md`。

---

## 三、正文骨架（十节，必须齐全）

````markdown
# <论文标题简称> 论文深度讲解

> **<论文完整英文标题>**
> <作者列表>
> <机构>
> <会议/期刊 + 年份> · arXiv: <编号> v<版本>（<日期>）
> 原文：[<pdf 文件名>](./<pdf 文件名>)
> 代码：<仓库 URL，或「未开源」>

---

## 1. 一句话定位

**<一段加粗的话，30 秒讲清做了什么、怎么做、效果多少。必须含关键数字。>**

这句话里的 N 个承重点，后面逐一拆解：

1. **<技术点 1>** — <2~4 句>
2. **<技术点 2>** — <2~4 句>
...

---

## 2. EDA 阶段映射（①-⑰）

参照 [芯片流程.md](../芯片流程.md) 的 17 阶段标准编号：

| 阶段 | 覆盖 | 说明 |
|------|:---:|------|
| **① RTL 设计** | ✅ **核心** | <为什么，用芯片流程的术语> |
| ② RTL 功能仿真 | ✅ | <…> |
| ③ 逻辑综合 | ❌ | <明确说明为什么不涉及，别留空> |
| …（⑰ 阶段全部列出，不涉及的用 ❌ 并给一句理由或「—」） |

**覆盖率：N/17。** <一句话概括这篇论文在流程上的定位>

> ⚠️ <该论文最容易被误读的边界。例如「它的『功能正确』只由 testbench 定义，PPA 完全不在优化目标里」>

---

## 3. 输入 / 输出

### 3.1 输入

<逐项说明数据类型 + 格式 + **可运行的最小具体例子**（Verilog 片段 / JSON / netlist 片段），
用 ```verilog / ```json / ```text 代码块。不要只写术语。>

### 3.2 中间产物

<如果流程有关键中间表示（AIG、CDFG、task graph、波形表…），在这里给出真实样例>

### 3.3 输出

<同样给具体例子，和 3.1 的输入首尾呼应，构成一个端到端 walkthrough>

---

## 4. 方法与架构

### 4.1 整体 Pipeline

<ASCII 框图。要求：>
- <数据流向清晰，用 │ ▼ ┌─┐ 绘制>
- <标注对应论文的 Figure 编号>
- <框内写「做什么」，框旁注「用什么工具/模型」>

### 4.2 核心模块逐个详解

<每个模块：做什么 → 为什么这样设计 → 输入输出是什么 → 关键实现细节>

### 4.3 模块职责矩阵

<表格：模块 | 输入 | 输出 | 用到的模型/工具 | 是否需训练>

---

## 5. 关键公式

<论文所有编号公式都要覆盖。格式严格遵守第四节「公式排版规范」。>

### 5.1 <公式名>

$$
<LaTeX>
$$

- $<符号>$：<中文含义>
- $<符号>$：<中文含义>

**工程直觉**：<这个公式在物理/工程上对应什么直觉>

**为什么这么设计**：<和替代方案比，好在哪>

---

## 6. 训练与实验设置

### 6.1 是否需要训练

<明确：不训练（推理时 Agent）/ SFT / RL(具体算法) / 从头训 GNN>
<若训练：数据规模、损失函数、超参、硬件、训练时长>

### 6.2 实验设置

<benchmark、baseline、评估指标定义（给公式）、消融设计>

### 6.3 关键实验结果

<论文的核心表格，用 markdown 表格重排。数字必须和 txt 原文一致。
最优值加粗。表下面写 2~3 句「这张表说明了什么」。>

---

## 7. 创新点

### 创新点 1：<标题 — 一句话概括>

<和已有方法的关键区别。要具体到「以前是 X，它改成 Y，因为 Z」。
避免「首次提出」这种空话，要说清楚首次提出的是什么机制。>

---

## 8. EDA 缩写表

| 缩写 | 全称 | 一句话解释 |
|---|---|---|
| AIG | AND-Inverter Graph | 与-非图，逻辑综合的标准中间表示，只有 2 输入 AND 和取反边 |

<本文出现的所有缩写都要收录，包括 EDA 缩写和 ML 缩写>

---

## 9. 与芯片流程的关系

### 9.1 在全流程中的位置

<回到 17 阶段，说清楚：接手谁的输出、交棒给谁、解决了什么痛点>

### 9.2 <「仿真通过」和「能做成芯片」之间隔着什么>

<这一节是本资料包的特色：拆解该论文声称的「成功」距离真实流片还差哪些环节>

---

## 10. 讨论与局限

### 10.1 论文自述的局限

### 10.2 代码/复现层面的问题

<从 *复现详解.md 提炼：论文口径与代码实现的差异、复现门槛（数据量/算力）、已知 bug。
每条要能说清「差在哪、影响什么结论」。>

### 10.3 本资料包的批判性分析

<你自己的分析：方法的隐含前提、评测的公平性、可扩展性天花板>

---

## 11. 复现信息

| 项目 | 内容 |
|---|---|
| 论文页 | <URL> |
| 代码 | <URL> + commit（若已审计） |
| 数据集 | <URL / 「未公开」> |
| 本地状态 | <已归档/已审计/未复现，沿用 模型梳理.md 的结论> |
| 复现等级 | <R0~R3，沿用原文档口径> |
| 主要门槛 | <数据量、算力、闭源工具依赖> |

---

## 12. 一分钟复述版

<8~12 行，把全文压缩成能对着讲的提纲。适合面试/组会复述。>
````

---

## 四、公式排版规范（严格执行，这是本次重点）

用户明确要求「公式格式排版不要乱七八糟」。硬规则：

### 4.1 独立公式

必须**单独占行**，`$$` 前后各空一行，`$$` 自己独占一行：

```markdown
上文段落。

$$
\mathcal{L} = -\sum_{i=1}^{N} y_i \log \hat{y}_i
$$

- $\mathcal{L}$：交叉熵损失
- $N$：样本数
```

**禁止**：`$$公式$$` 挤在一行；`$$` 和公式同行；`$$` 前后不空行。

### 4.2 行内公式

用单 `$`，且 `$` 紧贴内容：`当 $k=2$ 时` ✅ ／ `当 $ k=2 $ 时` ❌

### 4.3 符号解释

每个独立公式**下方紧跟符号列表**，用 `- $符号$：中文含义` 的无序列表。
符号必须包在 `$…$` 里，不要写成裸文本。

### 4.4 转义

- 下划线在数学模式内不用转义：`$x_i$`；在**普通文本**里的函数名要转义：`\_`
- `\text{}` 包裹公式里的中英文单词：`$\text{PassRate} = \frac{n_{\text{pass}}}{n_{\text{total}}}$`
- 多字符变量名用 `\text{}` 或 `\mathrm{}`，不要裸写（裸写会被渲染成斜体连乘）

### 4.5 禁用

- ❌ `\begin{align}`（多数 md 渲染器不支持）→ 用多个独立 `$$` 块，或 `\begin{aligned}` 包在 `$$` 内
- ❌ Unicode 数学符号混用（`∑` `∈` `≤`）→ 统一用 LaTeX（`\sum` `\in` `\leq`）
  例外：ASCII 框图和纯文本表格里可以用 Unicode 符号
- ❌ 公式里写中文（`$面积 = ...$`）→ 中文放到符号解释里

### 4.6 ASCII 框图

- 用 ``` 代码块包裹，语言标 `text`
- 对齐要用等宽思维检查：中文字符占 2 列
- 框线统一用 `┌ ─ ┐ │ └ ┘ ▼ ├ ┤`，不要混用 `+---+` 和 `┌───┐`

---

## 五、内容质量硬要求

1. **事实以 txt 原文为准。** 三份旧文档的数字若和原文冲突，改成原文的值，并在该处加
   `<!-- 修正：原 XX 文档写 A，原文 Table N 为 B -->` 注释。
2. **不要凭空编造。** 原文没有的公式不要硬造编号。若是你为了讲清机制而形式化的，
   必须显式写「论文未给出编号公式，以下是对其算法行为的形式化」。
3. **去重规则**：
   - 三份文档讲同一件事 → 保留信息量最大的那份表述，其余删掉
   - 「一句话定位」在旧文档中往往出现 2~3 次（开头 + 结尾）→ 只留第 1 节一处
   - 阶段映射表若旧文档有两份（一份 5 行简表、一份 17 行全表）→ 保留 17 行全表
   - 局限性若旧文档分散在多处 → 归并到第 10 节，按「论文自述 / 代码层面 / 你的分析」三类
4. **缩写**：正文首次出现给「全称（中文）+ 缩写」，之后直接用缩写；第 8 节汇总。
5. **不写套话**：删掉「近年来 AI 发展迅速」「具有重要意义」这类句子。
6. **交叉引用**：涉及同资料包其他论文时用相对链接，如 `[MAGE](../MAGE/论文深度讲解.md)`。
7. **篇幅**：正文不含代码块 15KB 起，核心论文（MAGE/ChipSeek/VerilogCoder/RTLCoder/
   DeepGate 系列/CircuitNet 系列/FluxBench/PDAGENT-BENCH）应达 30KB+。
   **但篇幅不是靠注水，是靠具体例子、真实数字、机制拆解。**

---

## 六、逻辑清晰度要求

用户明确要求「讲解思路要清楚明了」。每篇要满足：

1. **一条主线**：第 1 节点出 N 个技术承重点 → 第 4~7 节按同样顺序展开 → 第 12 节按同样顺序收束。
   读者应该能感到「这三处讲的是同一件事的三个层次」。
2. **端到端例子贯穿**：第 3 节选定一个具体例子（如 VerilogEval 的 `Prob014_andgate`），
   第 4 节的 pipeline 讲解、第 5 节的公式代入、第 6 节的结果都尽量用**同一个例子**。
3. **每节开头一句「这节要解决什么问题」**，不要直接甩表格。
4. **每个表格/框图下方 2~3 句解读**，不要让读者自己猜结论。
5. **先说结论再讲细节**：模块详解先写「这个模块存在的理由」，再写实现。

---

## 七、自检清单（写完每篇后逐条核对）

- [ ] 十二节齐全，没有空节
- [ ] 阶段映射表 17 行全列，覆盖率有数字
- [ ] 第 3 节有可运行的具体输入 + 对应输出例子
- [ ] 所有 `$$` 前后空行、独占行
- [ ] 每个独立公式下方有符号解释列表
- [ ] 没有 `\begin{align}`，没有 Unicode 数学符号混入公式
- [ ] ASCII 框图在 ```text 块内，框线风格统一
- [ ] 实验数字与 txt 原文逐个核对过
- [ ] 缩写表收录了正文所有缩写
- [ ] 旧三份文档的独有信息已全部并入（尤其是实验数据表和复现结论）
- [ ] 已删除 `*_标准化总结.md` 和 `模型梳理.md`
- [ ] 文末无重复的「一句话定位」
- [ ] 无 `<!-- glm-review -->` `<!-- kimi-review -->` 这类历史审阅注释残留
