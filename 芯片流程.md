# 数字芯片设计全流程（MAGE 17 阶段标准参考）

> 本文梳理自 MAGE 论文详解中的芯片流程章节，供所有论文标准化总结的「EDA 流程阶段」章节统一引用。
> 每个阶段包含：做什么、用什么工具、验证什么、输出什么。

---

## 十七阶段总览

```
阶段                                    验证手段
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
① RTL 设计           你写 Verilog
       │
       ▼
② RTL 功能仿真       iverilog/VCS + testbench
       │              验证：逻辑对不对？
       ▼
③ 逻辑综合            Design Compiler / Genus
       │              做什么：把 Verilog 翻译成门电路网表
       │              输出：门级网表 + SDF（延时）+ SDC（约束）
       │              验证：综合后立即跑初版 STA + 初版门级仿真
       ▼
④ 门级仿真            用 SDF 延时文件跑 testbench
       │              验证：翻译后逻辑还对吗？毛刺？时序？
       │              抓到：X传播、建立/保持违例、多驱冲突
       ▼
⑤ STA（静态时序分析） PrimeTime / Tempus
       │              验证：不跑仿真，纯数学计算所有路径延迟
       │              确认：每条路径延迟 < 时钟周期
       ▼
⑥ 形式验证            Formality / Conformal
       │              验证：综合后的网表和原始 RTL 逻辑等价吗？
       │              方法：数学证明（SAT/SMT），不是仿真
       ▼
⑦ 布局规划 (Floorplan)   决定：大模块摆哪、IO引脚、电源网络
       ▼
⑧ 标准单元摆放 (Placement) 把几百万个门大致摆到芯片上
       ▼
⑨ 时钟树综合 (CTS)        给时钟信号插缓冲器树，确保所有触发器同时触发
       ▼
⑩ 布线 (Routing)          用多个金属层连接所有门
       ▼
⑪ 后仿真               用 SDF + SPEF（寄生RC）跑门级仿真
       │              验证：真实走线延迟下，时序还好吗？
       ▼
⑫ 物理验证            DRC + LVS
       │              验证：造得出来吗？版图和网表一致吗？
       ▼
⑬ 签核 (Signoff)       STA/功耗/IR Drop 全部通过 → 生成 GDSII
       ▼
⑭ 流片                把 GDSII 发给台积电/三星/Intel
       ▼
⑮ 制造                光刻→刻蚀→掺杂→金属沉积... (3~6个月)
       ▼
⑯ 封装 + 测试          切晶圆→封装→ATE测试
       ▼
⑰ 芯片到手
```

---

## 各阶段详解

### ① RTL 设计

用 Verilog / VHDL 描述电路行为，只描述**行为**，不指定任何门电路。

```verilog
module counter(input clk, rst, output reg [3:0] count);
    always @(posedge clk)
        if (rst) count <= 0;
        else     count <= count + 1;
endmodule
```

**输出**：可综合的 RTL 代码。

---

### ② RTL 功能仿真

用仿真器（Icarus Verilog / Synopsys VCS / Siemens ModelSim）跑 testbench，验证**逻辑对不对**。

- 假设所有操作瞬间完成（零延迟），没有毛刺
- 只问一个问题：**数学上对不对？**
- Testbench = 给 DUT（被测电路）生成时钟、激励、读取输出并对比预期值的验证代码
- 信号值只有 0 和 1

**输出**：仿真波形、mismatch 报告。

---

### ③ 逻辑综合

把 Verilog 行为描述翻译成**具体的门电路网表**——数字芯片从"代码世界"跨入"物理世界"的关键桥梁。

三步：
1. **HDL → 通用逻辑表示**：`assign y = sel ? a : b` → 布尔方程 `y = (a∧sel)∨(b∧¬sel)`
2. **技术映射**：在标准单元库里匹配具体门型号（AND2X1、XOR2X1、FDRE…一个先进工艺库有 1000+ 种标准单元）
3. **优化**：在面积/速度/功耗约束下搜索最优网表

同一句 Verilog 可以有完全不同的门级实现：
- `y = sel ? a : b` → AND-OR 组合逻辑（4 个门）/ 传输门 MUX（6T，快 4 倍）/ 三态门 MUX（总线场景）
- `count + 1` → 脉动进位加法器（小而慢，64 位 ~3.2ns）/ 超前进位加法器（大而快，64 位 ~0.05ns）

综合工具根据 SDC 约束自动选择。

**输出**：门级网表 + SDF（每个门的延迟）+ SDC（设计约束）。

**关键概念**：

**SDC（设计约束）** — 你给综合工具的"需求说明书"：
```tcl
create_clock -period 2.0 -name clk [get_ports clk]   # 500MHz
set_input_delay -max 0.5 -clock clk [get_ports data_in]
set_output_delay -max 0.3 -clock clk [get_ports data_out]
```

**SDF（标准延时格式）** — 综合后每个门的真实延迟清单：
```
XOR2X1 u3: A→Y 上升延迟 0.05ns, 下降延迟 0.06ns
AND2X1 u4: A→Y 延迟 0.03ns
FDRE u1:  CLK→Q 延迟 0.10ns, 建立时间 0.05ns, 保持时间 0.02ns
```

**综合优化手段**：状态机重编码（二进制→独热码用面积换速度）、资源共享（合并互斥加法器）、重定时（挪寄存器位置均衡路径延迟）、常量传播+死代码消除。

---

### ④ 门级仿真

被测对象从 RTL 代码换成综合后的门级网表，跑**同一个 testbench**，但仿真器从 SDF 读取每个门的延迟（SDF 反标）。

信号值扩展到四种：**0、1、X（未知）、Z（高阻）**。

能抓到四类 RTL 仿真看不见的问题：

1. **毛刺（Glitch）**：不同门的延迟不同 → 信号到达有先后 → 输出短暂跳到错误电平
2. **建立/保持时间违例**：数据路径延迟太长 → 时钟沿采到的是半成品。建立时间 = 时钟沿前需稳定的时间，保持时间 = 时钟沿后需保持的时间——这是 D 触发器的物理极限
3. **X 传播**：没复位的寄存器初始是 X → 污染所有下游逻辑。真实芯片上电后寄存器确实是随机物理电平
4. **多驱冲突**：两个三态门同时驱动同一根线 → 短路风险 → 仿真报 X

---

### ⑤ STA（静态时序分析）

**不放电影，拿计算器把每条路径的长度全算一遍。**

- 门级仿真 = 放电影，只覆盖 testbench 跑到的场景
- STA = 扫描网表里每一条寄存器→寄存器路径，累加门延迟+走线延迟，和时钟周期比
- 输出 WNS（最差负余量）、TNS（总负余量）
- 覆盖 100% 路径，但不验证功能对不对

与门级仿真的关系：**互补**。门级仿真验证"你想得到的场景下功能对+时序对"，STA 验证"你没想到的路径上时序也 OK"。

**工艺角（Corner）**：制造偏差的极端情况 — SS（最慢）、FF（最快）、TT（典型）× 温度 × 电压 = 20+ 个分析场景。

---

### ⑥ 形式验证

用 SAT/SMT 求解器从数学上证明：**对所有可能的输入，RTL 和网表的输出完全相同。**

- 不跑 testbench，不看延迟，不关心毛刺
- 就证一件事：综合没有改变逻辑
- 因为综合可能做激进优化：状态机重编码、资源共享、重定时、死代码消除——网表结构可能和 RTL 完全不同

三级验证关系：
```
RTL 功能仿真（逻辑对不对，有穷）
  → 门级仿真（时序对不对，有穷但有延迟信息）
    → 形式验证（综合没改错？无穷——数学证明）
```

---

### ⑦ 布局规划 (Floorplan)

大件搁哪儿。CPU 核放中间，SRAM 宏单元放边上，IO 引脚围一圈，电源/地网络铺好。这一阶段基本决定了整个芯片的面积、功耗和性能边界。

---

### ⑧ 标准单元摆放 (Placement)

把几百万个标准单元（AND/XOR/DFF/…）放到芯片上各自的大致位置。目标是最小化后续布线长度。

---

### ⑨ 时钟树综合 (CTS)

让几十万个触发器"同时"收到时钟沿。

**两个物理问题**：
1. 一根线带不动几十万个负载 → 插缓冲器逐级放大驱动力（像电视台→省中转站→市差转台→每家每户）
2. 距离不同导致到达时间差（clock skew）→ 把快的路径故意拖慢（加缓冲器/加长走线），和慢的路径对齐

**代价**：时钟树占芯片面积 5~10%，占总功耗 20~40%（时钟一直在 GHz 频率翻转）。

---

### ⑩ 布线 (Routing)

用芯片的多个金属层，把千万个门之间的信号线逐一接通。同一层的线用通孔（via）换到另一层。

---

### ⑪ 后仿真

布线完成后，从物理版图提取寄生参数（RC Extraction），写成 **SPEF（标准寄生参数交换格式）**。

后仿真 = SDF（门延迟）+ SPEF（走线寄生 RC）一起跑 testbench。

**SDF + SPEF = 完整的"延迟真相"**：
- 门级仿真（只有 SDF）：走线延迟粗略估算
- 后仿真（SDF + SPEF）：真实走线 RC + 耦合电容串扰

在先进工艺里，走线延迟往往 > 门延迟。一根 1mm 的 M2 铜线 RC 延迟约 0.03~0.15ns。

**串扰（Crosstalk）**：相邻平行走线的耦合电容 → 一根线跳变时在邻居线上感应出"幽灵脉冲" → 如果恰好被时钟沿采到 → 错误锁存。

---

### ⑫ 物理验证 (DRC + LVS)

**DRC（设计规则检查）** — 能造出来吗？

工艺厂的每条 DRC 规则对应一个光刻物理极限：
- 金属线间距 ≥ 0.032μm（光刻分辨率极限）
- 通孔必须被上下两层金属各包围 ≥ 0.01μm（套准精度）
- 违规数目标：0

**LVS（版图 vs 原理图）** — 你画的和你设计的是一个东西吗？

- 从 GDS 版图"识别"出门电路 → 提取网表
- 和综合出的原始网表逐条对比（门数、线数、连接关系）
- DRC 通过只说明"图形能制造"；LVS 通过才说明"这真的是你想要的电路"

---

### ⑬ 签核 (Signoff)

一整套最终审核，每一项通过 → 生成 GDSII → 流片：

1. **STA 签核**：所有工艺角/温度/电压下，所有路径 Slack ≥ 0
2. **功耗签核**：动态功耗（½CV²fα）+ 静态功耗（漏电）在预算内
3. **IR Drop 签核**：最远角落电压降 < 电源电压的 5%（电压降 → 门变慢 → 吃时序余量）
4. **信号完整性签核**：串扰、地弹、EMI 在可接受范围
5. **生成 GDSII**：几百 GB 的几何图形文件，发给代工厂

---

### ⑭-⑰ 流片 → 制造 → 封装测试 → 芯片

- 光刻→刻蚀→掺杂→沉积→CMP 抛光，重复 50+ 层，3~6 个月
- 切割晶圆→封装→ATE 测试→分级→出货

---

## 核心物理概念

### 信号的本质

芯片上的 0 和 1 就是**电压高低**。1.2V = 1，0V = 0。

**0 变 1** = 驱动门打开 PMOS，电源 1.2V 接通到信号线上，电流给寄生电容充电，电压从 0V 爬到 1.2V。
**1 变 0** = 驱动门打开 NMOS，信号线接通到 GND，寄生电容放电，电压从 1.2V 放回 0V。

### RC 延迟

每根金属线都有寄生 R（电阻，阻碍电流）和 C（电容，储存电荷）。时间常数 τ = R×C。

- 一根 1mm 的 M2 铜线：R ≈ 200Ω，C ≈ 50fF → τ = 0.01ns，3τ = 0.03ns
- RTL 仿真假设 R=0、C=0 → 0ns 瞬间完成
- 芯片上有几百万根线，累积延迟决定最高频率

### D 触发器（记忆单元）

D 触发器 = RS 锁存器（双 NOR 门交叉反馈，双稳态 = 记忆）+ 时钟门控（精确控制何时更新）。

- 时钟上升沿：Q ← D（锁存新值）
- 时钟沿之间：Q 纹丝不动
- 建立时间（时钟沿前需稳定）+ 保持时间（时钟沿后需保持）= 物理极限

### 四种信号值

| 值 | 含义 | 场景 |
|----|------|------|
| 0 | 明确低电平 | 正常 |
| 1 | 明确高电平 | 正常 |
| Z | 高阻（断开） | 三态门关断、总线共享 |
| X | 未知 | 未初始化寄存器、多驱冲突 |

---

## 验证层次总结

| 阶段 | 新增物理信息 | 抓什么问题 |
|------|-------------|-----------|
| RTL 仿真 | 零延迟的理想世界 | 逻辑 bug |
| 逻辑综合 | 门延迟（SDF） | — |
| 门级仿真 | 门延迟注入 | 毛刺、X 传播、建立/保持违例 |
| STA | 所有路径的门延迟累加 | 100% 覆盖的时序违例 |
| 形式验证 | 无（纯数学） | 综合改错了逻辑 |
| 后仿真 | 走线寄生 RC（SPEF） | 真实走线延迟、串扰 |
| DRC+LVS | 版图几何结构 | 造不出来、版图网表不一致 |

---

> 本文是 MAGE 17 阶段标准参考。各论文标准化总结中「EDA 流程阶段」章节应使用本文的阶段编号（①-⑰）进行精确映射。
> 详细展开（含计数器从 Verilog 到硅片的完整 14 层推演）见 MAGE 详解原文。
