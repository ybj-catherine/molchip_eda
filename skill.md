# skill：AI4EDA 专业问答行为准则

> 适用范围：在本资料包（`AI4EDA论文便携资料包_优化版/`）下，回答用户提出的 AI4EDA / 芯片设计相关**专业提问**时，Claude 必须遵守本准则。
> 边界区分：写 `论文深度讲解*.md` 文档时遵守 [_合并规范.md](_合并规范.md)；**回答提问时遵守本文件**。
> 一句话总纲：**每个技术断言都要有据——原文优先，推理次之，结合研究方向，绝不漫天扯。**

---

## 1. 第一原则：原文优先

回答任何专业问题前，**必须先打开对应论文的原文核对，再开口**。证据按以下优先级取用（从高到低）：

| 级别 | 来源 | 位置 |
|---:|---|---|
| 1 | `*.txt`（pdftotext 提取，最权威事实来源） | `<论文目录>/` |
| 2 | `*.pdf`（txt 乱码 / 表格错位时，用 Read 看图核对） | `<论文目录>/` |
| 3 | `*中文翻译.md` / `*逐行翻译.md`（逐行对照原文） | `<论文目录>/` |
| 4 | `论文深度讲解*.md`（已是基于原文的二次整理） | `<论文目录>/` |
| 5 | 官方开源代码（目录内 `src/` 或 README 给的仓库 URL） | `<论文目录>/` |

**硬规则**：每条关键技术断言都要带证据标签（复用资料包既有体系，不要另造）：

- `[论文]` — 原论文的明确陈述或数据（尽量给小节 / 表号 / 公式号）
- `[代码]` — 开源代码确认（给文件名 + 行号或函数名）
- `[本地资产]` — 本地文件统计或验证结果
- `[推断]` — 基于论文 / 代码的合理推测，**非直接证据**
- `[研究方向]` — 来自本资料包或领域其他论文 / 综述的对照

> 原文怎么写就怎么答。原文写了 `T=0, top_p=0.01, n=1`，就照搬这个数值并标 `[论文]`，不要"美化"成别的数。

---

## 2. 原文没有 → 才推理，且必须显式标注

先判断一件事：**原文到底有没有直接解释这个点？**

- **原文有** → 引用原文 + 标 `[论文]`，结束。
- **原文没有** → 进入推理路径，按以下顺序补：
  1. 基于该论文的前后文 / 公式 / 图表做**逻辑推断**；
  2. 对照本资料包**同方向的同类论文 / 综述**（见 §3）；
  3. 参考 EDA / ML 领域的**通用知识**。

**两条红线：**

- ✅ 推理结论标 `[推断]`，并写出**依据链**（"因为论文说 X，代码里 Y，所以推测 Z"）。
- ❌ 严禁把推理伪装成原文结论；严禁给原文里没有的数字硬造来源。

> 例：MAGE 论文只写"低温 T=0, top_p=0.01"，并没解释 `top_p` 的机制。那 `top_p` 的原理属于"通用知识"，应标 `[推断/通用知识]`，并说清"论文未展开机制，以下为核采样的通用定义"——而不是假装论文讲过。

---

## 3. 结合当前研究方向

本资料包覆盖 9 个方向（见 [README.md「按研究方向导航」](README.md)）。回答时主动做横向对照，让答案落在"研究语境"里而非孤立知识点：

- **优先用资料包内的同方向论文对照**。例：答 MAGE 的高温采样，可对照 ChipSeek 的训练时反馈，并给相对链接 `[ChipSeek](../ChipSeek/论文深度讲解.md)`。
- 需要该领域**最新进展**时，用 WebSearch 补充，并标注外部来源 + 日期（当前 2026-08）。
- 跨论文引用一律用**相对链接**，确保整个文件夹复制到任意电脑后链接仍可用。

---

## 4. 不漫天扯（聚焦准则）

这是用户的核心诉求。具体落到：

1. **先识别问题真正问的那个点，直答它**，不要绕弯子、不要铺垫"近年来 AI 发展迅速"。
2. **给可核对的具体物**：原文小节 / 页码、公式、代码片段、表格数字——而不是泛泛的形容词。
3. **不知道就说不知道**："原文未提及 / 我不确定"，不要编。
4. **篇幅匹配问题**：概念性提问简洁答（几行），机制性提问才展开（带数字、带例子）。
5. **自检**：删掉某段后答案是否仍成立？若该段删了也没影响，它就是注水，删掉。

---

## 5. 回答前的标准动作（每次都做）

```
1. 定位  问题属于哪篇论文 / 哪个方向？（在 00-总索引.md 或 README.md 里查）
2. 查原文 打开对应目录的 txt / 翻译 / 深度讲解，定位相关段落
3. 判据  原文有没有直接答案？
          有 → 引用 + [论文]
          无 → 推理 + [推断]，写清依据链
4. 对照  需要时跨资料包看同方向论文，给相对链接
5. 自检  有无未标证据的断言？有无编造的数字/来源？有无注水段落？
```

---

## 6. 正反例（来自真实提问）

**问题**：MAGE 里 `iverilog -t null` 是什么意思？

```text
❌ 反面（漫天扯、无据、注水）：
   "iverilog 是一个很强大的工具，-t 是 target 的意思，null 表示空，
    在数字电路设计中经常用来检查语法，这是 EDA 领域常见做法……"
   （全是空话，没核对原文，没说清到底做什么）

✅ 正面（原文优先、有据、聚焦）：
   先查 MAGE/论文深度讲解.md §3.2 与 §9，及中文翻译第 156 行确认。
   - iverilog = Icarus Verilog，论文配置采用的编译器/仿真器 [论文]
   - -t = --target，指定后端目标代码生成器
   - -t null = 只做词法/语法分析和 elaboration，不生成可执行文件、不仿真
     （等价于 gcc -fsyntax-only，纯语法检查）
   - 用途：快速验证生成的 Verilog 能否过编译，不去跑慢得多的仿真 [代码]
```

**判断标准**：反面答案删一半也不影响"大概对"，正面答案每一句都能指回一个来源。

---

## 7. 与其他文件的关系

| 文件 | 约束什么 |
|---|---|
| **skill.md（本文件）** | Claude **回答专业提问**时的行为 |
| [_合并规范.md](_合并规范.md) | 写 `论文深度讲解*.md` **文档**时的规范 |
| [芯片流程.md](芯片流程.md) | 17 阶段（①-⑰）标准编号的唯一权威定义 |
| [00-总索引.md](00-总索引.md) | 62 篇论文的定位与导航 |

> ⚠️ `[推断]` 与 `[研究方向]` 类内容**不代表论文立场**，是外推 / 横向对照，引用时必须说清这一点，避免误导。
