# AI4EDA 论文知识库 — 管理指南

> 面向**仓库维护者（xll426）**：如何管理仓库、审核 PR、维护分支、同步内容。

---

## 目录

1. [仓库结构速览](#仓库结构速览)
2. [日常操作](#日常操作)
3. [PR 审核流程](#pr-审核流程)
4. [分支管理](#分支管理)
5. [团队管理](#团队管理)
6. [定期维护](#定期维护)
7. [应急处理](#应急处理)

---

## 仓库结构速览

```
xll426/molchip_eda
│
├── master 分支（公开）          ← 论文知识库主分支
│   ├── 00-总索引.md            ← 所有论文的索引和分类
│   ├── _合并规范.md            ← 深度讲解的 12 节写作规范
│   ├── 芯片流程.md             ← EDA 17 阶段标准定义
│   ├── _校验.py               ← 结构完整性自动校验脚本
│   ├── 使用手册.md             ← 面向使用者的操作指南
│   ├── CONTRIBUTING.md        ← 贡献指南
│   ├── README.md              ← 项目首页
│   ├── {论文名}/               ← 每篇论文一个目录
│   │   ├── 论文深度讲解.md
│   │   ├── *.pdf               ← 论文原文
│   │   └── *.txt               ← 文本提取
│   └── ...
│
└── internal 分支（半公开）      ← 内部协作，不对外展示
    ├── 团队阅读记录.md          ← 谁读了哪篇论文
    ├── 管理指南.md（本文件）     ← 维护者操作手册
    └── （未来可加：会议纪要、TODO、分工表等）
```

### 为什么用两个分支

| 分支 | 定位 | 谁可见 | 存什么 |
|------|------|--------|--------|
| `master` | 公开知识库 | 所有人 | 论文解读、索引、使用手册 |
| `internal` | 内部协作 | 团队成员 | 阅读记录、管理指南、内部讨论 |

**日常记住一件事**：改论文内容 → master；改阅读记录 → internal。

---

## 日常操作

### 自己加一篇新论文

```bash
cd molchip_eda
git checkout master                    # 在 master 上操作

# 1. 创建论文目录和文件
mkdir 新论文名
cp ~/Downloads/论文.pdf 新论文名/
# 提取文本、撰写深度讲解...

# 2. 更新索引
vim 00-总索引.md

# 3. 运行校验
python3 _校验.py

# 4. 提交推送
git add .
git commit -m "feat: 新增《XXX》论文深度讲解"
git push origin master
```

### 自己更新阅读记录

```bash
cd molchip_eda
git checkout internal                  # 切换到 internal

# 编辑团队阅读记录
vim 团队阅读记录.md

# 提交推送
git add 团队阅读记录.md
git commit -m "阅读记录: 更新XXX论文状态"
git push origin internal
```

### 同步 master 的变更到 internal

当 master 上有新论文加入后，internal 的阅读追踪表也需要更新：

```bash
git checkout internal
git merge master                       # 把 master 的新内容合并进来

# 如果有冲突（比如两边都改了同一个文件），手动解决：
#   git status           → 看哪些文件冲突
#   vim 冲突文件          → 编辑，保留两边需要的部分
#   git add 冲突文件
#   git commit

# 在新论文对应的行补充到 团队阅读记录.md
vim 团队阅读记录.md
git add 团队阅读记录.md
git commit -m "同步: 从 master 合并新论文，更新阅读追踪表"
git push origin internal
```

**建议频率**：每次在 master 上新加论文后，顺手做一次 merge。

---

## PR 审核流程

### 收到 PR 时做什么

#### 第 1 步：看 PR 类型

| PR 标题前缀 | 类型 | 审核重点 |
|------------|------|---------|
| `feat: 新增《XXX》` | 新论文 | 12 节完整性 + 数值准确性 |
| `fix(论文名): ...` | 修正错误 | 改动是否和原论文一致 |
| `chore: ...` | 杂项 | 不破坏现有内容即可 |

#### 第 2 步：逐项检查清单

**新论文 PR 必查项**：

```
□ 12 节结构完整（缺一节就 request changes）
□ 公式：$$...$$ 格式，前后有空行，有符号说明
□ 数值：用论文原文 txt 抽查 3-5 个关键数字
□ 措辞：搜索"待核实""推测""似乎""可能" — 有就驳回
□ §8 缩写表：包含所有专业缩写
□ §11 复现信息：依赖列表、关键步骤
□ 00-总索引.md 已更新
□ _校验.py 通过
□ 论文目录下有 PDF + txt（或至少 txt）
```

**修正 PR 必查项**：

```
□ 改动有原文依据（让他标注论文页/行号）
□ 没有引入新的数值错误
□ 措辞仍然是确定性的
```

#### 第 3 步：合并

```bash
# 方式一：在 GitHub 网页上点 "Merge pull request"（推荐）

# 方式二：命令行
git checkout master
git pull origin master
git merge --no-ff pr-branch-name
git push origin master
```

#### 第 4 步：合并后

```bash
# 同步到 internal
git checkout internal
git merge master
# 更新阅读追踪表（新论文默认标记为无人阅读）
git push origin internal
```

### 审核话术模板

**需要修改时**：
```
谢谢贡献！有几处需要调整：

1. §6 实验表格：Table 2 中的 XXX 数值与原文不一致，原文为 YYY
2. §5 公式：缺少符号说明
3. §10 讨论与局限：目前为空，请至少补充论文自述的局限

修改后 @我重新审核。
```

**通过时**：
```
LGTM！审核通过，已合并。感谢贡献 🎉
```

---

## 分支管理

### 分支策略

```
master
  ├── internal          ← 长期分支，定期从 master merge
  ├── paper/XXX         ← 贡献者加论文的分支（合并后删除）
  └── fix/XXX           ← 贡献者修 bug 的分支（合并后删除）
```

### 不需要的操作

- ❌ **不要从 internal 往 master 合并**（阅读记录不应该公开）
- ❌ **不要在 internal 上直接改论文内容**（论文内容只改 master）
- ❌ **不需要保护 internal 分支**（和 master 不一样，可以随意 push）

### 设置 Branch Protection（推荐）

在 GitHub 仓库 Settings → Branches → Add rule：

| 设置 | 值 |
|------|-----|
| Branch name pattern | `master` |
| Require a pull request before merging | ✅ |
| Require approvals | 1 |
| Dismiss stale reviews | ✅ |
| Require status checks to pass | 可选（如果配了 CI） |

> 这个设置的作用：别人不能直接 push 到 master，必须通过 PR + 你审批。你自己的 push 不受影响。

---

## 团队管理

### 添加协作者

GitHub 仓库 → Settings → Collaborators → Add people。

权限建议：

| 角色 | 权限 | 给谁 |
|------|------|------|
| **Admin** | 全部权限 | 你自己 |
| **Write** | 能 push、能 merge PR | 信任的核心维护者 |
| **Read** | 只能看、只能 fork | 其他人通过 PR 贡献 |

> 如果以后迁移到 GitHub Organization，改在 Organization → People 里管理。

### 内部团队怎么做

1. 每人 clone 仓库后，`git checkout internal` 拉取内部分支
2. 读完论文后在 `团队阅读记录.md` 里标记
3. 直接 push 到 internal（不需要 PR，因为 internal 不保护）
4. 定期看追踪表，找读过同一篇论文的同事讨论

### 内部讨论怎么记录

可以在 `internal` 分支上新建文件：

```
internal 分支
├── 团队阅读记录.md        ← 阅读状态
├── 管理指南.md           ← 本文件
├── 讨论记录/             ← （可选）会议纪要、TODO
│   └── 2026-08-08.md
└── 分工表.md             ← （可选）谁负责哪个方向
```

---

## 定期维护

### 每周（5 分钟）

```bash
# 1. 拉取最新
git checkout master && git pull
git checkout internal && git pull

# 2. 看有没有新的 Issue 或 PR
#    打开 https://github.com/xll426/molchip_eda/issues
#    打开 https://github.com/xll426/molchip_eda/pulls

# 3. 如果 internal 落后于 master，做同步
git checkout internal
git merge master
git push origin internal
```

### 每月（15 分钟）

```bash
# 1. 运行校验，确保没有断链
python3 _校验.py

# 2. 检查 00-总索引.md 的论文数量是否正确
grep -c "论文深度讲解" 00-总索引.md

# 3. 扫一眼 internal 的阅读记录，看看团队进度
git checkout internal
cat 团队阅读记录.md | grep "📖已读" | wc -l  # 已读论文数
```

### 每季度（30 分钟）

- 更新 README.md 中的论文总数和日期
- 检查是否有 arXiv 论文已被会议接收，更新会议信息
- 补充/更新 00-总索引.md 中的分类
- 和团队同步：哪些方向缺论文、哪些需要深读

---

## 应急处理

### 有人把 internal 内容误推到 master 了

```bash
git checkout master
git log --oneline -5              # 找到误推的 commit
git revert <误推的commit>          # 回滚
git push origin master
```

### master 和 internal 有冲突了

通常是两边改了同一个文件（比如都改了 00-总索引.md）：

```bash
git checkout internal
git merge master

# 如果有冲突：
#   <<<<<<< HEAD         ← internal 的版本
#   =======              ← 分界线
#   >>>>>>> master       ← master 的版本
# 手动编辑，保留需要的内容，删除标记行

git add .
git commit -m "merge: 解决 master 合并冲突"
git push origin internal
```

### 想回退到之前的版本

```bash
git log --oneline -10              # 找到目标 commit 的 hash
git checkout <commit-hash> -- 文件名  # 恢复单个文件
# 或
git revert <commit-hash>           # 回退整个 commit
```

---

## 附录：常用 Git 命令速查

```bash
# 查看当前在哪个分支
git branch

# 切换分支
git checkout master       # 到公开分支
git checkout internal     # 到内部分支

# 查看状态
git status

# 查看提交历史
git log --oneline -10

# 放弃本地修改
git checkout -- 文件名     # 放弃单个文件
git reset --hard HEAD     # 放弃所有修改

# stash 暂存（临时切换到别的分支时用）
git stash                 # 暂存当前修改
git checkout internal     # 干别的事
git checkout master       # 回来
git stash pop             # 恢复修改
```
