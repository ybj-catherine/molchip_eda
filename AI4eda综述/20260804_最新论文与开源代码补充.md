# AI4EDA 最新论文与开源代码补充（核验至 2026-08-04）

> 检索窗口：重点覆盖 2026-07-01—2026-08-03 新增论文，并回查此前论文后来补发的官方代码/数据。  
> 核验原则：优先采用 arXiv/OpenReview、作者或实验室 GitHub、Hugging Face/Zenodo 等一手入口；“论文公开”“代码可见”“有明确开源许可证”“端到端可复现”是四种不同状态。  
> 与本地索引的关系：本文是增量调研，不直接计入现有“38 篇逐论文详细解读”；只有在 PDF 归档、逐篇文档和本地证据审计完成后，才应增加该计数。

## 0. 检索与筛选方法

- 对 arXiv 2026-07-01—2026-08-03 的 RTL、Verilog、circuit、EDA、physical design、formal verification、PCB/RFIC 等组合词进行宽检索，初筛 57 条候选记录。
- 逐篇读标题、摘要、版本日期和论文中的 Code/Data Availability；排除纯软件代码生成、与电子设计无直接关系以及只有第三方聚合链接的结果。
- 对既有 2025—2026 工作反向检查作者组织、论文脚注、GitHub/Hugging Face/Zenodo，寻找论文发布后才补出的资产。
- 最终保留本文 24 项增量工作：10 项有某种官方代码/数据/artifact，14 项只有论文或尚未核验到稳定官方实现。开放状态只按 2026-08-04 快照判断。

## 1. 本轮结论

本轮找到的增量不是简单的“又多几篇 spec-to-RTL 论文”，而是出现了四条更值得补入综述的变化：

1. **更接近真实工程的 RTL benchmark**：ChipVerilog 把评测推进到 64 个来自 OpenCores 处理器、FPU、I2C 和 CORDIC 的局部、多模块与整系统任务；RTL-BenchLS 则强调 round-trip、masked design 与 repository issue，并使用形式等价证据。
2. **验证从生成 testbench 扩展到覆盖率、形式修复与定理证明**：GoGoTB、MechMem-RTL、Rtl2lean 和开放形式工具链研究分别覆盖 coverage closure、复用已验证修复机制、RTL→Lean 4、Yosys/SymbiYosys/Z3 反例闭环。
3. **Agent 评测开始同时考虑 PPA、跨阶段闭合和成本**：ARES 把推理 effort 与美元/token 成本纳入 RTL 优化；FluxBench 与 CLOSER-Bench 直接暴露 RTL-to-GDS、多阶段工具交互的失败模式。
4. **开放资产仍明显落后于论文数量**：截至核验日，真正新增且可确认官方代码/数据入口的重点项目主要是 ChipVerilog、RTL-BenchLS、CircuitEvo、StructRTL、RF-Agent、CircuitWeave、PDAGENT-BENCH 和 Weave；其中数个仓库没有检测到仓库级 LICENSE，不能仅因 GitHub 可访问就称为严格开源。

## 2. 已核验的新增官方代码、数据与 artifact

### 2.1 状态分级

| 状态 | 本文含义 |
|---|---|
| A | 官方代码、核心数据/评测器和明确许可证基本齐全；仍需固定 commit 和环境验证 |
| B | 官方仓库可见且有较实质资产，但许可证、权重、数据、工具或完整复现链至少缺一项 |
| C | 只有数据集、单一压缩包或匿名 artifact；不足以复现完整论文方法 |
| D | 只核验到论文；没有找到作者明确指向的当前公开实现 |

这一分级只描述**当前开放资产完整度**，不评价论文本身的学术质量。

### 2.2 项目表

| 工作 | 方向与主要资产 | 当前公开入口 | 开放性核验 | 建议定位 |
|---|---|---|---|---|
| [ChipVerilog](https://arxiv.org/abs/2607.13079) | 64 个 OpenCores 衍生 RTL 任务；含参考 RTL、提示、结果和评测工具；另有 Zenodo artifact | [GitHub](https://github.com/HKUSTGZ-MICS-LYU/ChipVerilog) | **B**：官方资产较完整，但核验时未检测到仓库级 LICENSE；部分 CORDIC/Yosys elaboration、模型任务数和 bounded equivalence 口径有 README 勘误 | 本轮最值得做本地静态审计和 smoke test 的 RTL benchmark |
| [RTL-BenchLS](https://arxiv.org/abs/2606.08976) | 大规模 RTL reasoning/generation benchmark 与评测框架 | [GitHub](https://github.com/hkust-zhiyao/RTL-BenchLS) | **B**：代码 MIT，数据/结果 CC BY 4.0，上游 RTL 保留原许可证；论文概念池为 10,028，当前仓库可直接看到的 release 文件为 420 条 slice、425 条 masked design 和 108 条 issue case，不能把概念池写成当前全部已下载样本；形式后端配置还涉及商业工具 | 修正原综述中“尚未发布”的过期判断；适合先跑数据解析和非商业部分 |
| [CircuitEvo](https://openreview.net/forum?id=DUtS9K9HH6) | LLM 演化 graph-structured program/AIG，调用 ABC 评估；含提示、示例和入口脚本 | [GitHub](https://github.com/MIRALab-USTC/CircuitEvo) | **B**：官方代码可见，但仓库体量较小且核验时未检测到仓库级 LICENSE；完整结果还依赖 LLM API 和 ABC | 逻辑综合/电路基础模型方向的重要增量，不应只放在 RTL 生成章节 |
| [StructRTL](https://arxiv.org/abs/2508.18730) | 从 RTL 的 CDFG 预测 post-synthesis area/delay；图自监督与跨阶段知识蒸馏 | [GitHub](https://github.com/cure-lab/StructRTL) | **B**：代码、数据目录和训练入口已公开；核验时仓库提交很少且未检测到仓库级 LICENSE | 补入 Circuit Foundation Model 与跨阶段 QoR 预测分类 |
| [RF-Agent](https://arxiv.org/abs/2607.18772) | RFIC 问答/设计推理，超过 11k QTSA；SFT、RAG、benchmark 与 Agent 框架 | [GitHub](https://github.com/Nina-nina123/RF-Agent) | **B**：仓库声明代码 MIT、数据与 benchmark CC BY 4.0；但训练语料来自教材与论文，实际再分发和商用前仍应逐项审计来源授权 | 扩充模拟/RFIC，而不是把 AI4EDA 等同于数字 RTL |
| [CircuitWeave](https://arxiv.org/abs/2607.23523) | 文本规格 + 原理图拓扑/行为 contract → RTL；论文称公开 5,000 个多模态 package | [Hugging Face 数据](https://huggingface.co/datasets/fengjiahao0421/CircuitWeave) | **C+**：已核验公开数据入口，未核验到完整方法代码和模型权重 | 值得跟踪多模态规格到 RTL；当前更适合做数据 schema 审计 |
| [PDAGENT-BENCH](https://arxiv.org/abs/2606.17253) | 353 个 VLSI 物理设计 Agent 问题、五类能力 | [Hugging Face](https://huggingface.co/datasets/PDAgent/PDAGENT-BENCH) | **C**：公开、非 gated，数据卡标 MIT；当前页面主要是一个约 38 MB 压缩包和极简 README，未核验到完整可执行工具环境/harness | 修正“仍只有未来开放承诺”；应写成“题目归档已放出，端到端环境未核验” |
| [Weave](https://arxiv.org/abs/2607.03835) | SPICE netlist → LTspice `.asc`，分层图布局并做 round-trip 连通性验证 | [GitHub](https://github.com/senolgulgonul/weave) | **B**：CLI/浏览器工具代码可见，但核验时未检测到仓库级 LICENSE | 模拟电路 schematic automation 的实用型补充，不是 LLM 训练项目 |
| [SINA](https://arxiv.org/abs/2607.01609) | 电路原理图图像 → 可机读网表；组件检测、连通性推断、OCR/VLM 和图同构检查 | [匿名 artifact](https://anonymous.4open.science/r/SINA-213F/README.md) | **C**：论文称 open-source，但目前核验到的是匿名评审入口；身份、长期稳定性和最终 release 都弱于正式项目仓库 | 可以列为 provisional artifact，不宜写成稳定官方开源项目，也不是“规格到模拟电路 Agent” |
| [VClare](https://arxiv.org/abs/2607.24854) | 面向不完整/不一致规格的 RTL 生成与规格修复 | [匿名 artifact](https://anonymous.4open.science/r/VClare/) | **C**：匿名入口只适合跟踪，不能替代带版本与许可证的正式 release | 待论文最终版和正式仓库出现后再升级开放状态 |

### 2.3 三个最容易被误写的细节

- **公开 GitHub ≠ 有开源许可证**：ChipVerilog、CircuitEvo、StructRTL、Weave 在本轮核验时均未检测到仓库级 LICENSE。可以说“官方代码公开可见”，不能无条件说“可自由修改和再分发”。
- **论文池规模 ≠ 当前仓库 release 规模**：RTL-BenchLS 的 10,028 是论文中的整体构建口径；当前仓库可见 release 文件只是其中的若干切片/任务集，引用结果时必须写 commit 和实际文件数量。
- **数据集出现 ≠ 工具环境已开放**：PDAGENT-BENCH 已不再是纯“未来开放承诺”，但当前 HF 页面不足以证明物理设计环境、checker、PDK/工具镜像均已公开。

## 3. 2026 年 7 月新增论文：建议补入综述、但暂未核验到稳定官方实现

### 3.1 P0/P1：最值得持续跟踪

| 日期 | 工作 | 应补入的核心贡献 | 当前边界 |
|---|---|---|---|
| 07-31 | [RTLCurator](https://arxiv.org/abs/2607.29283) | 行为感知、低标注成本的数据筛选；在 CodeV/RTLCoder 上只验证池中 10%，保留 80%，作者报告优于使用全量数据 | **D**；没有核验到作者官方代码。重点价值是纠正“只保留仿真全通过样本”的简单数据清洗观念 |
| 07-30 | [ARES](https://arxiv.org/abs/2607.27879) | 根据停滞状态自适应提升 reasoning effort，把 PPA FoM 与规范化美元/token 成本一起比较 | **D**；21 个训练设计、3 个未见测试设计的结论不能外推为普适策略 |
| 07-28 | [GoGoTB](https://arxiv.org/abs/2607.26181) | 规格约束的 Agentic RTL 验证和 coverage closure；同时报告 line/branch/toggle/functional coverage | **D**；未核验到代码，且 coverage 高不自动等于 bug-free |
| 07-20 | [FluxBench：Can AI Agents Really Complete RTL-to-GDS?](https://arxiv.org/abs/2607.17528) | 比较模型/Agent 架构在 RTL-to-GDS 工具工作流中的 token ROI、成功率和失败原因 | **D**；实验含商业工具和 PicoRV32，结果不能直接迁移到 OpenROAD 或任意设计 |
| 07-18 | [CLOSER-Bench](https://arxiv.org/abs/2607.16632) | 在预算约束下评 spec-to-RTL、RTL-to-GDS、spec-to-GDS 的跨阶段 design closure；使用 Harbor 封装并结合 Verilator/Yosys/OpenROAD/KLayout/Sky130 | **D**；论文是 10-task pilot，不能当成成熟大榜，尚未核验到公开 task package |
| 07-19 | [MechMem-RTL](https://arxiv.org/abs/2607.17053) | 把已经 verifier 确认的“修复机制”抽象成可复用 memory，而不是简单保存历史文本轨迹 | **D**；48 个任务、6 个模型仍属于有限评测；需要与同 token 的无记忆/RAG 基线比较 |
| 07-18 | [Rtl2lean](https://arxiv.org/abs/2607.16855) | SystemVerilog→Lean 4，分层生成 theorem/lemma 并复用证明；把验证证据推进到 proof assistant | **D**；论文报告 6 个设计、403 个 theorem，尚未核验到 translator/theorem library release |

### 3.2 P2：作为方向扩展纳入文献地图

| 日期 | 工作 | 方向 | 为什么值得保留/主要限制 |
|---|---|---|---|
| 07-30 | [Open-Source LLM-Driven Formal Verification](https://arxiv.org/abs/2607.28877) | Yosys + SymbiYosys + Z3 的多 Agent RTL 修复 | 价值在完整公开后端与失败分析；6 个 benchmark 只有 1 个可稳定修复，作者也将其定位为 feasibility study，不能包装成成熟系统 |
| 07-27 | [VPR-Evolve](https://arxiv.org/abs/2607.24998) | Agent 为具体 FPGA 设计演化 VPR 源码/算法 | 代表“改 EDA 工具本身”而非只调参数；当前只有 5 个 VTR-9 电路的有限评估且无已核验代码 |
| 07-20 | [A Progressive Approach to Synthesizable RTL Design Generation](https://arxiv.org/abs/2607.18519) | VeriRefine：显式 ASTF 中间规格，逐步精化再生成可综合 RTL | 重点是结构化规格精化；作者报告数字依赖 Claude Sonnet 4.6 与特定 benchmark，未核验代码 |
| 07-11 | [ChipFuzzer](https://arxiv.org/abs/2607.10340) | LLM 驱动语义 test/fuzz generation | 连接自然语言场景与 CPU RTL fuzzing；3 个 CPU 设计规模有限，未核验实现 |
| 07-04 | [RTL-Sequencer](https://arxiv.org/abs/2607.15830) | 从 RTL 序列预测时序 | DAC 2026；适合补入 pre-synthesis timing prediction，但不等于 STA/签核替代物 |
| 07-03 | [OmniLayout](https://arxiv.org/abs/2607.03261) | schematic-coupled PCB layout 多模态 benchmark | 1,681 个工业布局、77.24K placement instance 和四类任务扩大了 AI4EDA 到 PCB；尚未核验公开数据/代码 |
| 07-02 | [VeriChat](https://arxiv.org/abs/2607.01668) | RAG 多 Agent 硬件安全验证，调用 Icarus/Yosys/SymbiYosys | 适合放入 security verification 分支；未核验稳定官方仓库 |

## 4. 对现有三篇综述导读的实质补充

### 4.1 对 LLM4EDA 综述

原综述以“生成—验证—物理设计 Agent”为主线仍然成立，但需要增加两个正交维度：

- **成本维度**：不仅比较 pass rate/PPA，也固定 token、reasoning effort、EDA 调用次数和美元成本；ARES、FluxBench 是代表。
- **开放资产维度**：分别记录 paper/code/data/weights/evaluator/tool/PDK/license，不能再用一个“开源：是/否”字段压缩。

### 4.2 对 Circuit Foundation Model 综述

建议新增两类跨阶段表示：

- StructRTL 的 RTL CDFG、自监督表示和 synthesis-stage knowledge distillation；
- CircuitEvo 的 graph-structured program/AIG 与 ABC 确定性反馈。

这两类工作说明 circuit foundation model 不应只按“门级图预训练”理解，还包括 RTL→综合 QoR 的跨阶段表示，以及可以被 EDA 工具执行和演化的结构化程序表示。

### 4.3 对 Verilog 生成综述

建议把“规格→单模块 RTL”的主表扩成四条证据链：

1. **真实层次/系统任务**：ChipVerilog；
2. **数据筛选质量**：RTLCurator；
3. **多模态 topology/behavior contract**：CircuitWeave；
4. **验证与证明闭环**：GoGoTB、MechMem-RTL、Rtl2lean、开放形式工具链研究。

## 5. 建议的本地复现排期

| 优先级 | 项目 | 首轮动作 | 成功判据 | 主要风险 |
|---:|---|---|---|---|
| P0 | ChipVerilog | 固定官方 commit，审计目录、任务 manifest、许可证和参考 RTL 来源；先跑不调用 LLM 的 evaluator smoke test | 能从任务 ID 回溯 prompt/reference/checker，复现 README 中至少一个确定性检查 | 无仓库级 LICENSE；部分 reference/Yosys/有界等价有已知口径问题 |
| P1 | RTL-BenchLS | 统计当前实际 release 文件，跑 parser/unit test；把商业 formal 与可开源运行部分分开 | 本地记录精确样本数、hash、许可证和可运行 checker | 10,028 总池与当前切片混淆；完整形式环境可能依赖商业工具 |
| P1 | CircuitEvo | 核对 ABC 版本和输入真值表，先做不含大规模 API 成本的单例 smoke test | 脚本可生成候选并由 ABC 返回确定性 QoR | 无仓库级 LICENSE；API 模型和 Prompt 漂移 |
| P2 | StructRTL | 审计 data schema、split、依赖和训练入口，尝试单 batch 前向 | 记录 dataset 样本单位、图构建和 area/delay 标签来源 | 仓库较新、提交少、许可证未检测到 |
| P2 | RF-Agent | 先做数据 provenance/license 抽样，不立刻把语料用于训练 | 对每类来源建立可追溯清单，再跑 benchmark/harness | 论文/教材来源与二次数据许可可能需要额外确认 |
| 跟踪 | CircuitWeave / PDAGENT-BENCH | 定期检查是否补充代码、模型、harness、release/tag | 从 C 升级到 B/A 后再纳入正式复现队列 | 当前主要是数据入口或压缩包 |

## 6. 复现实验必须新增的记录字段

在现有 paper/code/data/evaluator 字段之外，建议统一加入：

- `asset_verified_at`：链接最后核验日期；
- `repo_commit`、`dataset_revision`、`archive_sha256`；
- `license_code`、`license_data`、`upstream_ip_license` 分列；
- `paper_total_pool` 与 `released_sample_count` 分列；
- `formal_backend`、`commercial_dependency`、`proof_scope`；
- `reasoning_effort`、`tokens`、`tool_calls`、`dollar_cost`；
- `paper_reported` 与 `locally_verified` 分列。

## 7. 本轮没有升级为“已开源”的工作

- FormalRTL：截至 2026-08-04 仍只核验到论文的未来开放表述，没有找到稳定官方仓库。
- ACE-RTL：没有核验到与论文明确绑定的完整官方 release。
- GoGoTB、ARES、FluxBench、CLOSER-Bench、MechMem-RTL、Rtl2lean：均只列论文，不使用“代码已开源”的措辞。
- 论文《Benchmarking LLMs for Verilog Design Flows》虽声称开放平台和 50 个任务，但本轮没有从论文元数据或检索结果中确认唯一作者官方仓库，因此暂不加入已核验仓库表。

## 8. 核验入口

- arXiv 论文页：以上表格均直链对应 `abs` 页面，可检查首次提交时间、版本和作者。
- GitHub：只采用论文明确链接或作者/实验室组织下可交叉确认的仓库。
- 数据托管：[CircuitWeave](https://huggingface.co/datasets/fengjiahao0421/CircuitWeave)、[PDAGENT-BENCH](https://huggingface.co/datasets/PDAgent/PDAGENT-BENCH)。
- 论文与仓库会继续变化；本文件反映的是 **2026-08-04** 快照，不应替代实验时再次固定 commit/revision/hash。
