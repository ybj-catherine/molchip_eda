                                                                                                                                                                                         1




                                                   ChatEDA: A Large Language Model Powered
                                                          Autonomous Agent for EDA
                                                  Haoyuan Wu† , Zhuolun He† , Xinyun Zhang, Xufeng Yao, Su Zheng, Haisheng Zheng, Bei Yu



                                            Abstract—The integration of a complex set of Electronic De-                           User Requirement
                                         sign Automation (EDA) tools to enhance interoperability is a
                                         critical concern for circuit designers. Recent advancements in                      ChatEDA
                                         large language models (LLMs) have showcased their exceptional                                                        Task Decomposition
arXiv:2308.10204v4 [cs.AR] 21 Sep 2024




                                         capabilities in natural language processing and comprehension,
                                         offering a novel approach to interfacing with EDA tools. This                                 AutoMage
                                         research paper introduces ChatEDA, an autonomous agent for                                                             Script Generation
                                         EDA empowered by an LLM, AutoMage, complemented by
                                         EDA tools serving as executors. ChatEDA streamlines the design
                                         flow from the Register-Transfer Level (RTL) to the Graphic
                                         Data System Version II (GDSII) by effectively managing task                                                             Task Execution
                                         decomposition, script generation, and task execution. Through
                                         comprehensive experimental evaluations, ChatEDA has demon-                                                                EDA Tools
                                         strated its proficiency in handling diverse requirements, and our
                                                                                                                                   API Specifications
                                         fine-tuned AutoMage model has exhibited superior performance
                                         compared to GPT-4 and other similar LLMs.
                                                                                                                                                               RTL         GDSII
                                           Index Terms—Electronic design automation, large language
                                         models, machine learning algorithms.                                          Fig. 1 Overview of AutoMage powered ChatEDA. With Au-
                                                                                                                       toMage as the controller and EDA tools as the executors, the
                                                                I. I NTRODUCTION                                       workflow consists of three stages: 1) Task Decomposition; 2)
                                                                                                                       Script Generation; 3) Task Execution.
                                         E     LECTRONIC Design Automation (EDA) encompasses a
                                               crucial set of software tools utilized for circuit design,
                                         analysis, and verification. These tools are organized within a                language. This allows these models to demonstrate remarkable
                                         complex design flow, featuring intricate programming inter-                   accuracy and fluency in a variety of NLP tasks, such as
                                         faces. Notably, advanced RTL-to-GDSII design platforms like                   natural language understanding and generation. To leverage
                                         OpenROAD [1] and iEDA [2] consist of numerous procedures                      their potential in specialized domains [10]–[12], instruction
                                         and adjustable parameters. Commercial tools, with their exten-                tuning [13] fine-tunes LLMs with domain-specific corpora,
                                         sive functionalities and options, offer even more comprehen-                  resulting in remarkable performance on these specialized do-
                                         sive capabilities. Circuit design engineers employ these tools                mains. Specifically, Vicuna [14], Guanaco [15], and Orca [16],
                                         iteratively to achieve their design objectives, often resorting to            have applied instruction tuning to train LLMs, making use
                                         custom scripts for specific operations. Conventionally, scripting             of the outputs produced by the GPT, and thereby achieving
                                         languages such as TCL have been the de facto means of                         significant outcomes
                                         interacting with EDA tools [3], which is tedious and prone                       Furthermore, scholars have initiated exploration into the
                                         to errors. Experienced design teams often adopt tools from                    incorporation of tools or models into LLMs. Toolformer [17], a
                                         different vendors, greatly increasing the difficulty in creating              groundbreaking methodology, integrates external API tags into
                                         and maintaining such scripts.                                                 text sequences, thus facilitating LLMs to connect with external
                                            Recently, the field of natural language processing (NLP) has               tools. This tool utilization, coupled with the capacity for logical
                                         undergone a revolutionary transformation with the emergence                   reasoning, broadens the LLM’s potential as a robust general
                                         of large language models (LLMs) such as GPT-3 [4], GPT-4                      problem solver. Several proof-of-concept demonstrations, in-
                                         [5], Claude2 [6], and Llama [7], [8]. Extensive training on large             cluding AutoGPT [18] and BabyAGI [19] serve as motivational
                                         corpora enables large language models to acquire emergent                     illustrations. The current implementation of LLMs in toolchain
                                         abilities [9] by learning intricate patterns and relationships in             automation predominantly relies on generic LLMs without spe-
                                                                                                                       cific fine-tuning. However, such LLMs, lacking bespoke fine-
                                             † Equal contributors                                                      tuning, are unable to consistently meet performance standards
                                             This work is supported in part by The Research Grants Council of Hong
                                         Kong SAR (Project No. CUHK14210723). (Corresponding author: Bei Yu)           tailored to users’ specific requirements [20]. Particularly in
                                             Haoyuan Wu and Haisheng Zheng are with Shanghai Artificial Intelligence   the EDA domain, LLMs exhibit limited familiarity with EDA
                                         Lab, Shanghai, China.                                                         toolchains, leading to frequent errors during the tool usage
                                             Zhuolun He, Xinyun Zhang, Xufeng Yao, Su Zheng, and Bei Yu are with
                                         the Department of Computer Science and Engineering, The Chinese University    process. In this paper, we introduce the expert EDA LLMs, the
                                         of Hong Kong, NT, Hong Kong SAR.                                              AutoMage series, which have been optimized for proficiency
                                                                                                                                        2



with EDA tools, thereby enhancing the stability and reliability       In this phase, model parameters are refined to augment the
of the automation of EDA workflows.                                   likelihood of predicting ensuing tokens in sequences [24]. Such
   In this work, we propose ChatEDA, an expert LLM system             a strategy equips these models with a deep comprehension of
designed to generate code for manipulating EDA tools based on         linguistic intricacies. As training data swells, the proficiency of
natural language instructions. To be more specific, as illustrated    these models in text generation augments [4].
in Fig. 1, ChatEDA is an LLM-driven autonomous agent system              Significantly, while these models are not engineered for
for EDA, functioning as the agent’s intellectual hub, responding      distinct downstream tasks, their broad knowledge garnered
to human instructions and manipulating the EDA tools via              during pretraining paves the way for stellar performance across
APIs to deliver autonomous Register-Transfer Level (RTL) to           diverse tasks. This is achieved with minimal fine-tuning on
Graphic Data System Version II (GDSII) capabilities without           scantily labeled datasets [4]. Their pre-trained representations
necessitating any code writing. To guarantee the performance,         coupled with the decoder-centric design empower them to craft
we utilize the AutoMage series (AutoMage and AutoMage2) as            coherent, sensible, and fluid prose infused with reasoning. The
the control unit of the ChatEDA. AutoMage is an expert LLM            blend of generative prowess and pre-acquired knowledge paves
that specializes in the utilization of EDA tools, which is barely     the path for imaginative text generation.
learned in open-source LLMs. To further enhance AutoMage’s               Prominent LLMs, including GPT-4 [5], PaLM [25], [26],
abilities in real-world environments, we propose the upgraded         and LLaMA [7], [8], epitomize GPLMs. They display unparal-
version of AutoMage, AutoMage2. Our contributions are listed          leled generalization and few-shot learning prowess, endorsing
as follows:                                                           a plethora of text generation applications. In the context of
   • ChatEDA, the first LLM-powered EDA interfacing frame-            this paper, AutoMage is fine-tuned based on GPLMs (Llama2),
      work and methodology;                                           underlining the adaptability and potency to automate the RTL-
   • AutoMage series (AutoMage and AutoMage2) are fined-              GDSII flow in real-world scenarios.
      tuned based LLMs and purpose-built to enhance the capa-
      bilities of ChatEDA; and                                        B. Low-Rank Adaptation of LLMs
   • Comprehensive evaluations using ChatEDA-Bench to                 LLMs are characterized by their vast number of parameters,
      show the superior performance of AutoMage series, sur-          making full fine-tuning of these parameters during training
      passing GPT-4 and other well-known LLMs in various              impractical. An efficient alternative is low-rank adaptation
      tasks.                                                          (LoRA) [27], a technique that involves preserving the pretrained
   The remainder of this article is organized as follows. Sec-        model weights while introducing trainable low-rank decompo-
tion II discusses the preliminaries, including a brief illustration   sition matrices into each layer of the Transformer architecture.
of the generative pre-trained language model, low-rank adapta-        This method substantially decreases the number of trainable
tion of LLMs, in-context learning ability of LLMs, and block-         parameters for subsequent tasks.
wise k-bit quantization technique for efficient training. Our            The Transformer architecture comprises numerous fully con-
proposed LLM-powered framework for EDA will be explained              nected layers that conduct matrix multiplications with full-rank
in Section III. AutoMage and its upgraded version AutoMage2           weight matrices. Despite the complexity, pre-trained language
will be elaborated in Section IV and Section V separately.            models demonstrate a low intrinsic dimension, allowing them
Section VI demonstrates our experiment setup, evaluation of           to learn efficiently even after random projection into a smaller
our methods, quantitative comparisons, and some case studies,         subspace [28]. Consequently, for a pre-trained weight matrix
followed by a discussion about limitations and future work in         represented as W ∈ Rh×d , it can be updated using a low-rank
Section VII and a conclusion in Section VIII.                         decomposition W + ∆W = W + L1 L2 , where L1 ∈ Rh×r ,
                                                                      L2 ∈ Rr×d , and the rank r ≪ min(h, d).
                      II. P RELIMINARIES                                 During the training process, W remains fixed and does not
A. Generative Pre-Trained Language Model                              receive gradient updates, whereas the matrices L1 and L2 are
Generative pre-trained language models (GPLMs) stand at the           endowed with trainable parameters. For the equation y = W x,
pinnacle of natural language processing advancements. Unlike          the modified forward pass is expressed as:
conventional models such as BERT [21] and XLNet [22],
                                                                                   y = W x + ∆W x = W x + L1 L2 x.                   (1)
which utilize an encoder-decoder architecture, GPLMs [4]–
[8] employ a neural network structure exclusively comprising          Initially, L2 is initialized with a random Gaussian distribution,
decoder blocks based on the transformer architecture. This            and L1 is set to zero, ensuring that ∆W = L1 L2 is zero at
exclusive decoder-only design offers several advantages over          the outset.
traditional encoder-decoder architectures [23]. By eschewing             During production deployment, W = W + L1 L2 is explic-
the encoder’s compression of input into a singular vector,            itly computed and stored for regular inference. Importantly, this
GPLMs adeptly capture long-range language dependencies,               method incurs no additional latency when compared to a fully
leading to the synthesis of more coherent prose. The auto-            fine-tuned model, making it an efficient choice for practical
regressive decoder forecasts each token considering its entire        applications.
antecedent context, ensuring fluid and logical text generation.          In the context of this study, LoRA is applied to streamline the
   GPLMs are trained through self-supervised learning on ex-          fine-tuning process for Llama2. By preserving the pre-trained
pansive corpora, cultivating broad language representations.          weights and solely updating the low-rank matrices, the number
                                                                                                                                     3



of trainable parameters is significantly reduced, enhancing the      where c represents the quantization constant. The inverse oper-
model’s efficiency and applicability in real-world scenarios.        ation dequantization is defined as:
                                                                                                        X Int8
C. In-Context Learning                                                           dequant cFP32 , X Int8 = FP32 = X FP32 .        (4)
                                                                                                           c
The concept of in-context learning (ICL) [29] exemplifies
                                                                     The input tensor X ∈ Rb×h is divided into n contiguous
the remarkable ability of LLMs to execute downstream tasks
                                                                     blocks of size B by flattening the tensor and segmenting the
effectively by conditioning on the in-context prompt containing
                                                                     linear structure into n = (b × h)/B blocks. These blocks
a limited number of input-output examples, all without explicit
                                                                     are independently quantized using Equation (3), creating a
fine-tuning. For instance, when presented with a task like
                                                                     quantized tensor and n quantization constants ci .
predicting nationalities, a prompt featuring sample input names
                                                                        In the context of efficient fine-tuning of quantized LLMs,
and their respective nationalities, such as “Albert Einstein was
                                                                     a strategy involving block-wise k-bit quantization is applied.
German. Mahatma Gandhi was Indian. Marie Curie was             ”,
                                                                     To be more specific, the pretrained LLM is stored in a 4-bit
allows LLMs to correctly fill in the blank with the appropriate
                                                                     datatype using Equation (3), and then dequantized from 4-bit
nationality.
                                                                     to 16-bit datatype for forward and backward pass computations
   The phenomenon of ICL arises from the presence of long-
                                                                     using Equation (4).
range coherence in pre-training documents. During the pre-
training phase, LLMs are compelled to deduce the latent               III. C HAT EDA: LLM- POWERED F RAMEWORK FOR EDA
concept denoted by θ across multiple sentences, ensuring coher-
ent continuations. When provided with the in-context prompt,         ChatEDA, an LLM powered agent, is specifically designed
denoted as xic , ICL manifests when LLMs deduce shared               for RTL-to-GDSII flow automation. The main objective of
concepts within xic to make predictions denoted as xo .              ChatEDA is to understand and respond to user requirements in
   Assuming that LLMs precisely capture the pre-train distribu-      natural language. In order to achieve this, ChatEDA is capable
tion p with adequate data and expressivity [30], ICL involves        of breaking down complex user requirements into smaller, more
characterizing the conditional distribution of completions given     manageable sub-tasks and subsequently utilizing appropriate
in-context prompt, denoted as p(xo |xic ), under p. This is the      EDA tools to address them.
posterior predictive distribution, which marginalizes out latent        As illustrated in Fig. 1, AutoMage, an LLM fine-tuned with
concepts as follows:                                                 EDA expert knowledge, serves as the central processing unit
                          Z                                          of ChatEDA. After receiving a natural language requirement
             p(xo |xic ) = p(xo |θ, xic )p(θ|xic )d(θ).       (2)    from the user, AutoMage first interprets the requirement and
                          θ
                                                                     decomposes it into a set of sub-tasks, known as task de-
In scenarios where p(θ|xic ) focuses on the concepts within          composition. Then, based on the decomposed smaller tasks
xic with more input-output examples, LLMs learn through              and the specifications for the external tools, e.g., OpenROAD,
marginalization by effectively “selecting” the concept of input-     AutoMage generates Python scripts for accomplishing these
output examples from xic .                                           tasks. Ultimately, ChatEDA executes the generated script to
   ICL [31] presents an efficient and adaptable method to            get the final output for the user requirement. We will detail the
leverage the knowledge and capabilities embedded within ex-          workflow and the training process of AutoMage in the following
tensively pre-trained language models. It stands as a promising      sections.
paradigm enabling LLMs to learn from a minimal set of
examples during inference. In our research, we employ ICL            A. Task Decomposition
for self instruction and the collection of instruction datasets to   In the realm of automating the RTL-to-GDSII flow through
fine-tune Llama2.                                                    EDA tools, numerous user requests often entail intricate inten-
                                                                     tions. A fundamental requirement lies in the agent’s ability to
D. Block-wise k-bit Quantization
                                                                     comprehend these complex human natural language requests.
Quantization [32], a process of discretizing input from a high-      Thanks to the robust capabilities of AutoMage, ChatEDA
information representation to a lower-information one, involves      adeptly interprets these tasks based on human-defined speci-
converting data types with more bits into those with fewer bits,     fications.
such as transitioning from 32-bit floats to 8-bit integers. Block-      Considering the intricate nature of automating the RTL-to-
wise k-bit quantization ensures optimal utilization of the low-      GDSII flow, it becomes imperative to break down the overar-
bit data type’s entire range. This approach normalizes the input     ching task into a series of manageable sub-tasks to achieve the
data type within the target data type’s range using the absolute     desired outcome. Therefore, we introduce task decomposition
maximum of the input elements, typically organized as a tensor.      as the primary stage of ChatEDA. In this phase, AutoMage
For instance, quantizing a 32-bit Floating Point (FP32) tensor       assesses user requirements and dissects them into a sequence
into an Int8 tensor within the range of [−127, 127] can be           of structured tasks. Fig. 2 illustrates the task decomposition
expressed as:                                                        process. For instance, when faced with a convoluted and lengthy
                                                     
                                    127                              natural language request, ChatEDA employs AutoMage to
            X Int8 = round                     X FP32
                               absmax(X FP32 )                 (3)   break it down into a series of specific sub-tasks. These tasks,
                   = round(c  FP32
                                   ·X FP32
                                           ),                        encompassing aspects like logic synthesis, floorplan, placement,
                                                                                                                                      4




                                                       #1: User Requirement
      For the design named "aes" on the platform "asap7", please perform synthesis with a clock period of 5, followed
      by floorplan with a core utilization of 70%. Then, execute placement with a density of 0.8. Next, proceed CTS to fix
      40% of violating paths. Finally, evaluate the performance after routing using "power" metric.

                      #2: Task Decomposition                                          #3: Script Generation
        task1: set up the EDA tool
                                                                      # Initialize
        func: set_up()                                                eda = chateda()
        args:
          design_name: "aes"               task5: perform CTS         # Set up the EDA tool
          platform: "asap7"                func: cts()                eda.setup(design_name="aes", platform="asap7")
                                           args:
                                             tns_end_percent: 40      # Perform synthesis
        task2: perform synthesis                                      eda.run_synthesis(clock_period=5)
        func: run_synthesis()
        args:                                                         # Execute floorplan
          clock_period: 5                  task6: perform routing     eda.floorplan(core_utilization=70)
                                           func: global_route()
                                                 detail_route()       # Perform placement
        task3: execute floorplan                                      eda.placement(density=0.8)
        func: floorplan()
        args:                                                         # Perform CTS
                                           task7: evaluation          eda.cts(tns_end_percent=40)
          core_utilization: 70
                                           func: get_metric()
                                           args:                      # Perform routing
        task4: perform placement             stage: "route"           eda.global_route()
        func: placement()                                             eda.detail_route()
                                             metrics: ["power"]
        args:
                                                                      # Evaluate the performance after routing
          density: 0.8                                                Performance = eda.get_metric(“route”, ["power"])




Fig. 2 Language functions as a conduit enabling ChatEDA to integrate EDA tools for resolving complex EDA tasks. Within the
framework, ChatEDA acts as the controller that harmonizes and orchestrates the collaboration among various tools. ChatEDA first
formulates a task list derived from user requirements, subsequently generating scripts corresponding to these decomposed tasks.

and others, can then be efficiently handled through various EDA       executor. With its proven efficacy in both script generation and
tools.                                                                task execution, ChatEDA stands as a pivotal system in ensuring
B. Script Generation                                                  reliable automation of the RTL-to-GDSII flow.
Upon the completion of the task decomposition phase, manage-          IV. AUTO M AGE : LLM- BASED C ONTROLLER OF C HAT EDA
able sub-tasks are defined, facilitating the streamlined orchestra-
tion of the complex task. Each sub-task is executable through         A. Base Model Selection
corresponding APIs within the EDA tools. Consequently, the            The AutoMage model is a fine-tuned version of Llama2 [8],
need arises to craft a script that invokes these APIs for task        which is designed based on the standard Transformer model
execution. During the script generation phase, depicted in            architecture in a decoder-only setup, meaning each timestep can
Fig. 2, a structured text incorporating API specifications, user      only attend to itself and past timesteps. It is worth noting that
requirements, and the decomposed sub-tasks serves as input            CodeLlama [33] models achieve strong performance in coding
for AutoMage. Subsequently, AutoMage generates a Python               ability, which are incrementally pretrained on code resources
script, ready for direct execution. This script enables the RTL-      based on Llama2 models. However, during the process of
to-GDSII flow, promoting efficient architectural exploration,         incremental pretraining on code resources, it will lose a lot of
design space evaluation, early Quality of Results (QoR) es-           general knowledge including EDA knowledge. To understand
timation, and detailed physical design implementation.                user requirements on EDA tool usage, ChatEDA needs to have
                                                                      basic EDA knowledge. As a result, we utilize Llama2 models
C. Task Execution
                                                                      for this research.
Following script generation, ChatEDA executes the script using
the Python interpreter, and the sub-tasks are then performed uti-     B. Instruction Tuning for AutoMage.
lizing EDA tools. According to the setup procedures, ChatEDA          To guarantee the reliability of ChatEDA, knowing when and
sets environment variables accordingly. Then, it launches a           how to use the tools, which is determined by the LLM’s
subprocess that runs the tool (i.e., OpenROAD in our im-              capability, is of vital importance. AutoMage is an expert LLM
plementation) script executor. Internally, the Python wrapper         specializing in utilizing EDA tools, which are barely learned in
implements different functionalities by specifying relevant Tcl       open-source LLMs. To integrate expert knowledge into LLMs,
scripts or commands and running them by the tool script               instruction tuning is an effective approach that enables LLMs
                                                                                                                                  5



                                                                           S ELF - INSTRUCTION P ROMPT
                                  y               AutoMage
                                                                        You are an expert who can automate the RTL to GDSII
                                                                        ,→  flow by using a fully autonomous toolchain. You are
                                      L2                                ,→  invited to teach us how to create chips by using a
                                                                            fully autonomous toolchain for digital layout
                             W
                                                                        ,→
                                                                        ,→  generation across die sizes, process nodes, and
                                      L1           QLoRA
                                                                        ,→  foundry options.
                                                                        As a result, you are now required to generate instances
  In-Context Examples                                                   ,→  for EDA user requirements and the corresponding
                                  x                                     ,→  script generation by given APIs. I will provide you
                                                                        ,→  with the template of the instance, available Python
                                                                        ,→  APIs, and some instance examples to describe how to
                                                                        ,→  use APIs to generate scripts.
                                                                        I need to learn how to use these APIs according to your
                                                    Llama2              ,→  generated instances.

                                                                        The instance template is as follows:
        GPT-3.5/4       Instruction Pool                                <Requirement>:
                                                                            This part is the requirement.
                                                                            You should try to make it diverse as if they are
Fig. 3 Overview of Instruction Tuning. During the instruction               ,→  written by different people.
tuning process, we use the self instruction paradigm to construct           Try to customize needs to cover APIs as much as
                                                                            ,→  possible.
our instruction pool via GPT models. Then we apply the                  <Analysis>:
QLoRA technique for efficient instruction fine-tuning.                      This part is the analysis.
                                                                            According to the provided APIs, you are required to
                                                                            ,→  provide an analysis to illustrate how to
to benefit from the pairing of domain-specific natural language             ,→  fulfill user requirements.
                                                                        <Script>
descriptions and their corresponding responses. Therefore, we               This part is the script.
incorporate instruction tuning to train AutoMage, the core con-             You should only contain Python code with correct
                                                                            ,→  API calls.
troller of ChatEDA, based on the open-source LLMs (Llama2               </Script>
[8]). As shown in Fig. 3, the process of instruction tuning
                                                                        Here are the provided APIs of class 'chateda':
mainly includes three parts, self instruction, instruction collec-      <<<API documents>>>
tion, and instruction fine-tuning, which are detailed as follows.
                                                                        Here are some restrictions:
                                                                        1. The instances should be in English.
Self Instruction. To enhance instruction tuning, it is imperative       2. Try not to repeat the verb or noun for each
to gather high-quality instructions that can effectively educate        ,→  requirement and explanation to maximize diversity.
                                                                        3. The requirements can be either an imperative
Large Language Models (LLMs) on the utilization of EDA                  ,→  sentence or a question. For example, you can
tools through APIs. Given the costliness of obtaining high-             ,→  combine questions with imperative requirements.
                                                                        4. Your requirements and relative analyses should be
quality instructions, the self-instruction paradigm has gained          ,→  professional and logical.
significant traction in recent research endeavors [34]. This            5. The analyses should be as detailed as possible. For
                                                                        ,→  example, you can think it step by step.
paradigm revolves around the utilization of diverse in-context          6. The requirements are required to be able to be
prompts, tailored to specific instances, to query GPT-3.5/4 and         ,→  completed using the provided APIs.
                                                                        7. In the script, you are required to use named
automatically generate additional instances from them.                  ,→  arguments in the Python code to allow passing
                                                                        ,→  arguments by specifying their names, independent of
   The self-instruction paradigm is meticulously crafted, draw-         ,→  their positions.
ing inspiration from the ICL [31] capability of GPT-3.5/4. In           8. The Python code in the scripts can be executed
                                                                        ,→  directly.
this methodology, we provide explicit guidance to the model             9. You can't skip any steps that need to be executed.
through APIs, incorporating necessary restrictions within the           ,→  The usual flow of chip designing goes like this in
                                                                        ,→  sequence (Note that each step follows can't be
self-instruction prompts. Subsequently, we employ instances             ,→  executed unless the previous step has been
structured in the format <requirement, decomposition, script>           ,→  executed.): a. Setup; b. Synthesis; c.
                                                                        ,→  Floorplanning; d. Placement; e. Clock Tree
as in-context prompts, enabling precise alignment with GPT-             ,→  Synthesis (CTS); f. Global Routing; g. Detailed
3.5/4. According to the formulation outlined in Equation (2),           ,→  Routing; h. Density Fill; i. Final Report;
                                                                        10. Noted that if you are required to get a metric, You
GPT-3.5/4 generates instances that directly reference the in-           ,→  have to execute that process first.
stances provided within the in-context prompts. This approach
                                                                        Now I need you to teach me how to master these APIs
not only refines the understanding of the given instructions            ,→  according to the provided instances. Please
but also enhances the model’s ability to generate contextually          ,→  generate more different instances according to the
                                                                        ,→  provided instance examples under the provided
relevant responses.                                                     ,→  restrictions.
   Herein lies the template for the self-instruction prompt,
                                                                        Here are some instance examples:
meticulously designed to maximize the efficiency of instructing         <<<in-context instances>>>
LLMs in utilizing EDA tools via APIs. The template, marked by
careful structuring and explicit directives, plays a pivotal role in
shaping the in-context learning process of GPT-3.5/4, ensuring         Instruction Collection. In adherence to the self-instruction
the acquisition of high-quality instructions and fostering the         paradigm, a meticulously crafted dataset of approximately
model’s adeptness in the EDA domain.                                   1500 instances was formulated for the explicit purpose of
                                                                       instruction tuning. It is crucial to note that both GPT-3.5
                                                                                                                                                                                                                 6



   <<<Requirement>>>
   I am designing a "sensor_interface" module on the "nangate45" platform. Can you guide me on
                                                                                                           <<<Requirement>>>
   optimizing the clock period during synthesis, the core utilization during floorplanning, and the        Perform floorplanning for the "image_processor" design on the platform "nangate45" with a core
   placement density to achieve the best power performance after placement?                                utilization of 75%, core aspect ratio of 1.3, and core margins of 2.


    <<<Response>>>                                                                                         <<<Response>>>
    To optimize the power performance of the "sensor_interface" module on the "nangate45"                  To perform floorplanning for the "image_processor" design on the platform "nangate45" with
    platform, we need to adjust the clock period during synthesis, the core utilization during             specific parameters, we need to go through the following steps:
    floorplanning, and the placement density. By tuning these parameters, we can find the optimal          1. Setup: Use the "setup()" function to specify the details of the design. In this case, the design
    configuration that maximizes power eﬃciency after placement. Here"s a step-by-step guide to            name is "image_processor," and the platform is "nangate45."
    fulfill these requirements:                                                                            2. Synthesis: Before floorplanning, we need to run synthesis using the "run_synthesis()" function.
    1. Set up the EDA tool for the "sensor_interface" module on the "nangate45" platform.                  3. Floorplanning: Perform floorplanning using the "floorplan()" function. We set the parameters
    2. Run synthesis with diﬀerent clock periods and evaluate the power performance.                       "core_utilization", "core_aspect_ratio", and "core_margins" to the given values.
    3. Perform floorplanning with varying core utilization values to analyze the impact on power           Here"s the script to perform floorplanning for the "image_processor" design on the platform
    consumption.                                                                                           "nangate45" with the specified parameters:
    4. Adjust the placement density and observe the eﬀect on the power performance.
    5. Iterate the tuning process by adjusting the clock period, core utilization, and placement density   def perform_floorplanning(
    until finding the optimal configuration.                                                                   design_name,
                                                                                                               platform,
    Here"s the Python script to perform this optimization:
                                                                                                               core_utilization,
                                                                                                               core_aspect_ratio,
    # Define the optimization function                                                                         core_margins
    def optimize_power(clock_period, core_utilization, density):                                           ):
        # Create an instance of the chateda class                                                              # Create an instance of the chateda class
        eda_tool = chateda()                                                                                   eda_tool = chateda()
        # Setup the EDA tool for the sensor_interface module on the nangate45 platform                         # Setup the design
        eda_tool.setup(design_name="sensor_interface", platform="nangate45")                                   eda_tool.setup(design_name=design_name, platform=platform)
        # Run synthesis with the specified clock period                                                        # Run Synthesis
        eda_tool.run_synthesis(clock_period=clock_period)                                                      eda_tool.run_synthesis()
        # Perform floorplanning with the specified core utilization                                            # Perform Floorplanning with the specified parameters
        eda_tool.floorplan(core_utilization=core_utilization)                                                  eda_tool.floorplan(
        # Perform placement with the specified density                                                             core_utilization=core_utilization,
        eda_tool.placement(density=density)                                                                        core_aspect_ratio=core_aspect_ratio,
        # Get the power metric after placement                                                                     core_margins=core_margins
        power = eda_tool.get_metric("place", ["power"])                                                        )
        return power                                                                                       # Call the function to perform floorplanning for the "image_processor" design with the
    # Define the parameter ranges                                                                          specified parameters
    param_ranges = {                                                                                       perform_floorplanning(
        "clock_period": {"minmax": [1, 20], "step": 1},                                                        design_name="image_processor",
        "core_utilization": {"minmax": [30, 90], "step": 0},                                                   platform="nangate45",
        "density": {"minmax": [0.05, 0.95], "step": 0}                                                         core_utilization=75,
    }                                                                                                          core_aspect_ratio=1.3,
    # Tune the parameters using the tune() function                                                            core_margins=2
    tune(optimize_power, param_ranges)                                                                     )




Fig. 4 Examples of generated EDA tools instructions. Moreover, we also provide more examples in the repo https://github.com/
wuhy68/ChatEDAv1 for a better understanding of the generated dataset.

and GPT-4, while powerful, are not infallible, occasionally                                                 ploying a unique token to demarcate these segments effectively.
producing erroneous data. To counter this, a portion of the                                                 Consequently, an auto-regressive objective is implemented,
dataset was manually curated or refined to ensure accuracy and                                              effectively nullifying the loss of tokens originating from the
reliability. Specifically, we first automatically verified whether                                          user requirement. This strategic approach confines the back-
our designed OpenROAD API interface, an automated process,                                                  propagation process solely to the response tokens, enhancing
could correctly execute the generated code. In cases where the                                              the precision and efficiency of the fine-tuning process.
code was inexecutable, we modified it to ensure functionality.                                              C. Efficient Fine-tuning of Quantized LLMs
Subsequently, we manually evaluated whether the generated
                                                                                                            In addressing the critical necessity for swift training procedures,
code satisfied the task requirements specified in the prompt. If
                                                                                                            our study incorporates the QLoRA technique [15], aiming to
the generated code failed to meet the requirements or exhibited
                                                                                                            expedite the fine-tuning process efficiently. QLoRA employs 4-
an incorrect thought process, we manually edited it. For the
                                                                                                            bit Normalfloat (NF4) Quantization and Double Quantization
entire dataset, we dedicated approximately two person-days to
                                                                                                            methodologies, ensuring the attainment of high-quality 4-bit
meticulously validate and refine the requirement-response pairs,
                                                                                                            fine-tuning. This innovative approach is intricately coupled with
ensuring quality and accuracy, resulting in a refined dataset of
                                                                                                            paged optimizers, which serve the crucial purpose of mitigating
approximately 1500 training samples.
                                                                                                            memory spikes during gradient checkpointing, thus averting
   We provide some examples of the generated instruction                                                    potential out-of-memory errors. The notable efficacy of QLoRA
dataset in Fig. 4.                                                                                          is pivotal in enabling us to ensure the seamless performance
                                                                                                            of instruction fine-tuning utilizing Large Language Models on
Instruction Fine-tuning. In the process of fine-tuning, each in-
                                                                                                            a substantial scale, specifically 34B/70B models, a milestone
stance consists of a requirement and a corresponding response.
                                                                                                            traditionally hindered by memory overhead constraints.
The response involves a detailed decomposition process and
                                                                                                               To delve deeper into the components of QLoRA, it is im-
script. In this context, we view the prompt and requirement
                                                                                                            perative to elucidate the essence of the following fundamental
as crucial instructions that direct LLMs to generate accurate
                                                                                                            components: NF4 quantization and double quantization.
corresponding responses. During instruction fine-tuning, we
utilize a commonly used prompt in the style of Alpaca [35],                                                 NF4 Quantization. The NormalFloat (NF) data type, an exten-
and AutoMage is trained on approximately 1500 instances to                                                  sion of Quantile Quantization [36], emerges as an information-
learn how to utilize EDA tools.                                                                             theoretically optimal method, ensuring equal distribution of
   To ensure an appropriate length for the model sequences,                                                 values within each quantization bin of the input tensor. Quantile
requirements and responses extracted from the entire training                                               quantization achieves this uniformity by estimating the quantile
set are concatenated. This concatenation is performed while em-                                             of the input tensor through the empirical cumulative distribution
                                                                                                                                      7



function.                                                           beam search with a beam width set to 4. This strategic choice
                                                                    significantly elevates the precision and quality of text generation
Double Quantization. This sophisticated process involves
                                                                    by enabling the model to explore a wider range of possibilities
quantizing the quantization constants to achieve additional
                                                                    and select the most suitable output.
memory savings. While precise 4-bit quantization [37] neces-
sitates a small block size, it inevitably results in significant     V. AUTO M AGE 2: A N U PGRADED V ERSION OF AUTO M AGE
memory overhead. Double quantization [15], a pivotal inno-          AutoMage, fine-tuned with datasets comprising approximately
vation, involves employing the quantization constants cFP32 2       1500 EDA tool instructions, may exhibit overfitting to these
from the first quantization as inputs for a second quantization     limited datasets. Furthermore, Llama2 exhibits deficiencies in
step. This process yields quantized quantization constants cFP8
                                                             2      coding and logical reasoning capabilities [38]. Consequently,
and a secondary set of quantization constants cFP32
                                                  1   . For the     AutoMage inherits these limitations undoubtedly. Considering
second quantization, we utilize 8-bit Floats with a block size      that real-world environments present greater challenges, there
of 256, ensuring optimal memory usage without compromising          are still opportunities to further enhance AutoMage’s abilities.
performance. Notably, to facilitate symmetric quantization, we      AutoMage2 is an upgraded version of AutoMage that aims to be
center the positive cFP32
                      2    values around zero after subtracting     a more stable and capable controller of ChatEDA. Our objective
the mean before quantization.                                       is to augment AutoMage’s capabilities, particularly in logical
QLoRA. Combining these aforementioned components,                   reasoning for task decomposition and script generation coding,
QLoRA enhances a linear projection within a transformer             to enhance the system’s overall performance. We adopt the
layer of LLMs through an additional factorized projection, thus     model architecture (Section IV-A), efficient fine-tuning process
contributing to the efficiency and effectiveness of fine-tuning     (Section IV-C), and auto-regressive decoding (Section IV-D)
processes in the realm of LLMs. As shown in the dash box of         from AutoMage, and employ three key techniques to improve
Fig. 3, given a linear projection, y = Wx, the computation,         its capabilities: enriched training corpus, instruction tuning with
based on Equation (1), is as follows:                               explanation [16], and chain of thoughts (CoT) prompting [39].
       y BF16 = doubleDequant(cFP32
                               1    xBF16 , ck-bit
                                             2 ,W
                                                   NF4
                                                       )            A. Enriched Training Corpus.
                                                              (5)   AutoMage’s capabilities stem largely from the pretraining of
              +LBF16
                1    LBF16
                      2    xBF16 ,
                                                                    the underlying LLMs. To enhance performance, we must also
where W is original weights and L1 and L2 are ad-
                                                                    enhance the base LLMs.
ditional QLoRA weights. The final weights can be com-
                                                                       First, a high-quality corpus is critical for instruction tun-
bined without extra inference costs. Here, the function
                                                                    ing. With only around 1500 instances generated by the self
doubleDequant(cFP32
               1    , ck-bit
                       2 ,W
                             k-bit
                                   ) can be obtained by:
                                                                    instruction paradigm (Section IV-B), AutoMage achieved high
        dequant(dequant(cFP32
                         1    , ck-bit
                                 2 ), W
                                        4bit
                                             ) = WBF16 ,      (6)   performance. To augment the data, we not only added more
The matrix W is of size NF4 and is quantized to 4-bit precision.    EDA tool usage instances but also filtered to ensure quality
The constant c2 is of size FP8. We choose a blocksize of 64 for     and semantic diversity. Specifically, we had an instructor [40]
W to achieve higher quantization precision, and a blocksize of      for encoding the generated instructions, calculated cosine sim-
256 for c2 to conserve memory. This choice is based on recent       ilarity, and removed those with a similarity score above 0.95.
results showing that 8-bit quantization does not significantly      After deduplication, we obtained around 1500 instances.
degrade performance compared to 16-bit quantization.                   Moreover, code corpus represents a highly abstract language
   In summary, QLoRA employs a singular storage data type,          containing complex logical constructs. Exposure to such corpus
typically NF4, alongside a computational data type represented      can strengthen LLMs’ coding skills and logical reasoning. To
by 16-bit BrainFloat (BF16). The forward and backward passes        strategically bolster the coding and reasoning proficiencies of
entail dequantizing the storage data type to the computational      the LLMs, we amalgamated approximately 110k instances of
data type. Notably, during these processes, weight gradients        code instructions [38] sourced from open-access repositories
are exclusively computed for the LoRA parameters utilizing          with our own generated instances of EDA tools instructions
the BF16 representation.                                            after deduplication. This amalgamation resulted in the creation
                                                                    of hybrid instruction datasets. These hybrid datasets, rich in
D. Auto-Regressive Decoding
                                                                    diversity and complexity, were instrumental in fine-tuning the
Beam search constitutes a fundamental algorithm employed
                                                                    instructions provided to AutoMage2, thereby augmenting their
across numerous NLP models, serving as the pivotal decision-
                                                                    overall comprehension and proficiency in utilizing EDA tools
making layer responsible for selecting the optimal output
                                                                    effectively. This amalgamation not only broadened the scope of
concerning predetermined target variables, such as maximum
                                                                    the instructions but also fostered a comprehensive understand-
probability or the subsequent output character. This method fa-
                                                                    ing of the intricate nuances associated with coding and EDA,
cilitates the simultaneous consideration of multiple tokens for a
                                                                    ensuring a robust and well-rounded training process.
specific position within a given sequence, relying on conditional
probability assessments. The selection process is governed by a     B. Instruction Tuning with Explanation.
key hyperparameter, denoted as beam width, which determines         The training corpus contains teacher (GPT-3.5/4) responses that
the number of N-best alternatives to be considered. In our          explain the reasoning process, providing additional learning
auto-regressive decoding process of AutoMage, we implement          signals beyond the prompt-response pairs used in vanilla in-
                                                                                                                                     8



struction tuning (Section IV-B) for AutoMage. To elicit such        paged AdamW 8-bit optimizer [36], initiating with a learning
explanations, we employ system instructions for instruction         rate of 1 × 10−4 , no weight decay, a batch size of 128, and a
tuning with the explanation of AutoMage2 following Orca [16]        sequence length of 4096 tokens. Ultimately, the model is fine-
style (e.g. “think step-by-step” and “justify your steps”), which   tuned for 1 epoch on 16×A100 with 80G memory each.
can better enable mimicking the teacher’s thought process. The         During the inference phase, the user‘s requirement prompts
intricately designed prompt is presented below for reference:       in natural language. These can be designed for a simple task
                                                                    (e.g. “Perform routing for the processor design on the asap7
    I NSTRUCTION F INE - TUNING P ROMPT                             platform.”) or delineate a broader, more general goal (e.g.
 ### System:                                                        “Please show me how to complete the design flow in the
 You are AI assistant, capable of utilizing numerous
 ,→  tools and functions. User will give you a task.                script.”). The output of AutoMage2 is the executable script.
 ,→  Your job is to generate a Python script to complete               As for evaluation, we consider notable LLMs including
 ,→  the task using the provided tools and functions.
 ,→  While performing the task think step-by-step and               Claude2 [6], GPT-3.5 [4], and GPT-4 [5] as our baselines for
 ,→  justify your steps.                                            performance assessment. To ensure a comprehensive compari-
 You have access to the following tools and functions:
 chateda is an autonomous tool that can automate the RTL            son, we utilize different LLMs as the core controllers for our
 ,→  to GDSII flow by executing steps through                       autonomous agent, ChatEDA. The target API is a simplified
 ,→  tools(functions) with various parameters.
 tune is a function that can perform parameter tuning.              Python wrapper of OpenROAD [1].
 Specifically, you have access to the following details
 ,→  of the provided tools and functions:                              For better understanding and easy reproduction of our work,
 <<<API documents>>>                                                we provide ChatEDA-bench (Section VI-B), examples of EDA
 ### User:
 <<<Requirement>>>                                                  tool instructions dataset, and API document with its corre-
 ### Assistant:                                                     sponding OpenROAD implementation in the open-source repo
 <<<Response>>>
                                                                    https://github.com/wuhy68/ChatEDAv1.

C. Chain of Thoughts.                                               B. ChatEDA-Bench
CoT prompting [39] enables complex reasoning through inter-         To assess the effectiveness of AutoMage2, we have developed
mediate reasoning steps. Zero-shot CoT prompting [41] builds        ChatEDA-Bench, a comprehensive evaluation benchmark com-
on this by introducing a simple zero-shot prompt: appending         prising 50 distinct tasks spanning three distinct categories: sim-
“Let’s think step by step.” This prompts LLMs to generate a         ple flow calls (30%), complex flow calls (30%), and parameter
CoT that answers the question. From this, more accurate an-         flow calls (40%). The diversity of these tasks ensures a rigorous
swers can be extracted. Consequently, we apply zero-shot CoT        evaluation of AutoMage2’s capabilities across various scenar-
during inference of AutoMage models and other notable LLMs          ios. In the subsequent sections, we will elucidate two examples
to obtain more precise responses. The intricately designed zero-    from each task category, shedding light on the intricate nature
shot CoT prompt is presented below for reference:                   of the evaluation scenarios and the challenges posed by each
                                                                    case.
    Z ERO -S HOT C OT P ROMPT
                                                                    Simple Flow Calls The first task requires the successful exe-
 ### System:
 You are AI assistant, capable of utilizing numerous                cution of the whole process, including evaluation. These cases
 ,→  tools and functions. User will give you a task.                test the fundamental application of LLMs and their sequence
 ,→  Your job is to generate a Python script to complete
 ,→  the task using the provided tools and functions.               of usage with the API interfaces.
 While performing the task think step-by-step and
 ,→  justify your steps.                                            Case 1: I want to carry out clock tree synthesis for the
 You have access to the following tools and functions:              ,→  design "modulator.v" using the process design kit "gf180"
 chateda is an autonomous tool that can automate the RTL            ,→  with a desired density of 0.95. Can you help me to
 ,→  to GDSII flow by executing steps through                       ,→  generate the script?
 ,→  tools(functions) with various parameters.
 tune is a function that can perform parameter tuning.              Case 2: For the "aes" circuit, I want to run the steps from
 Specifically, you have access to the following details             ,→  setup to detailed routing on the platform "asap7"?
 ,→  of the provided tools and functions:
 <<<API documents>>>
 ### User:
 <<<Requirement>>>
 Let's first describe and explain what the task is
                                                                    Complex Flow Calls These cases heavily rely on logic, includ-
 ,→  asking. Then, analyze how to complete the task step            ing traversing parameters, further examining the LLM’s logical
 ,→  by step using the provided tools and functions.
 ,→  Finally, generate the Python script according to
                                                                    reasoning and understanding of each API argument.
 ,→  your analysis.
 ### Assistant:                                                     Case 1: I want to perform a grid search on the design "how"
 <<<Response>>>                                                     ,→  on the "gf180" platform for floorplan parameters, CTS
                                                                    ,→  parameters, and placement parameters to find the best
                                                                    ,→  balance of chip area, power consumption and performance.
                                                                    ,→  Can you help me to do that?
                      VI. E XPERIMENTS
                                                                    Case 2: I have a design called "data_processor" on the
A. Setup                                                            ,→  platform "asap7". I want to experiment with different
For efficient fine-tuning of AutoMage2, we implement a con-         ,→  combinations of clock periods, density values, halo
                                                                    ,→  sizes, and channel widths during the whole EDA flow.
stant learning rate schedule with a 0.03 warm-up ratio using
                                                                                                                                                                        9



Parameter Tuner Calls These cases require the LLM to                                                                                                          Grade A
                                                                           AutoMage2                             82.0                         8.0 10.0
provide a parameter-tuning solution, thoroughly testing the                                                                                                   Grade B
                                                                            AutoMage                         74.0                       12.0     14.0         Grade C
LLM’s logic and use of EDA tools.
Case 1: I want to perform DSE for the design "aaksdjka" using                   GPT-4                     62.0                   16.0          22.0
,→  the technology node "asap7", and consider tns, wns, the
,→  required chip surface area, and the power consumption at                  Claude2              46.0                  26.0                 28.0
,→  the final stage as the evaluation metric of DSE. The
,→  search space of parameters are as follows:                                GPT-3.5       28.0             18.0                 54.0
1. Core utilization ranging from 60% to 80% with a step of
                                                                                        0          20            40        60            80             100
,→  5%.                                                                                                           % Percentage
2. Core aspect ratio from 1 to 3 continuously.
3. Core margins from 2 to 10 with a step of 1.                      Fig. 5 Evaluation results for AutoMage [42] and AutoMage2
4. Macro place halo from 5 to 15 with a step of 1.
5. Macro place channel from 5 to 15 with a step of 1.
                                                                    compared to other LLMs. AutoMage models outperform other
6. Clock period of 5.                                               notable LLMs by a significant margin in task planning and
7. Density from 0.3 to 0.9 with a step of 0.
8. 50% TNS end percent.
                                                                    script generation and AutoMage2 performs the best.

Case 2: I want to perform DSE for the design "aaksdjka" using       GPT-4 demonstrates the advances of our method in grading
,→  the technology node "asap7", and consider tns, wns, the
,→  required chip surface area, and the power consumption at        performance. Our experiment affirms AutoMage2 as the current
,→  the final stage as the evaluation metric of DSE. The            state-of-the-art LLM for this evaluation task, which will be the
,→  search space of parameters includes placement parameters
,→  and clock tree synthesis parameters.                            most reliable controller of ChatEDA.
                                                                    D. Case Studies
C. Evaluation of LLMs                                               In this section, we analyze five tasks involving user interac-
An objective evaluation system was developed to assess the          tions with EDA tools through a conversational interface. It is
task decomposition and script generation capabilities of LLMs       important to note that this exploration is not exhaustive due to
designed for automated script generation. An evaluation system      the limitations inherent in this qualitative study. For instance,
was developed to assess the task decomposition and script           controlling all possible variables, such as the vast array of
generation capabilities of LLMs designed for automated script       responses the LLM can generate for a given prompt, is beyond
generation. This evaluation system comprises two integral com-      our reach. As a result, our conclusions are based on a selected
ponents. Initially, the Python scripts generated are subjected      group of samples deemed representative. Nonetheless, we con-
to testing through the EDA tool interface to determine their        tend that the description of these exemplars will enhance our
executability. Subsequently, a manual assessment is conducted       understanding of the robust performance of AutoMage2, which
to ascertain whether the responses from the LLMs meet the           enhances the reliability of AutoMage2 powered ChatEDA.
users’ requirements. To maintain fairness and accuracy in the       For each task, we first outline the user’s requirements and
evaluation process, multiple judges’ perspectives are taken into    subsequently present the task decomposition process and the
account, and these judges are kept unaware of which LLM             scripts generated by AutoMage2. Moreover, given that GPT-4
generated the response during scoring. The system uses a three-     is the best LLM in baselines during the script evaluation, we
tiered grading scheme, with Grade A representing the highest        also provide the scripts generated by GPT-4 for comparison.
achievement. Grade A is awarded to LLMs that demonstrate co-
herent task decomposition and generate accurate scripts. Grade      Task 1: Parameter Grid Search. The first task requires LLMs
B indicates respectable but imperfect performance, assigned to      to execute a grid search, heavily relying on logic like traversing
LLMs that plan logically but falter in script generation. Grade C   parameters. This further examines the LLM’s grasp of each API
denotes failure in both task decomposition and code generation.     argument. AutoMage2 performed excellently, demonstrating its
   During the evaluation process, we use ChatEDA-Bench for          logical prowess and exemplary API argument comprehension.
a comprehensive inspection. As summarized in Fig. 5, Au-            In contrast, GPT-4 showed a deficient understanding of param-
toMage models outperform all notable LLMs, and our proposed         eter combination optimization. This highlights AutoMage2’s
AutoMage2 achieved the best performance, correctly earning          more sophisticated grasp of the EDA tools’ interface and
Grade A for 82% of test cases. This significantly exceeds the       parameter tuning logic compared to GPT-4.
62% Grade A attained by the next highest performer of no-           U SER : I want to perform a grid search on the design “how”
table LLMs, GPT-4. While GPT-4 exhibited reasonably strong          on “gf180” platform for floorplan parameters, CTS parameters
capabilities, it struggled to differentiate between lower quality   and placement parameters to find the best balance of chip area,
responses, assigning Grade B and C more evenly at 16% and           power consumption and performance. Can you help me to do
22% respectively. In contrast, AutoMage2 reliably identified        that?
the highest grade-worthy responses. The other LLMs, Claude2
and GPT-3.5, performed noticeably worse than AutoMage2,             AUTO M AGE 2:
with Claude2 achieving 46% Grade A and GPT-3.5 only 28%
Grade A. This highlights their comparative deficiencies. In             TASK D ECOMPOSITION
summary, AutoMage2 substantially outperformed GPT-4 and              This task requires us to perform a grid search over a set of parameters for
                                                                    ,→ the floorplan, CTS, and placement stages of the chip design flow. The
other LLMs by consistently assigning appropriate high grades.       ,→ parameters to be tuned are:

The considerable margin between AutoMage2 and second-place
                                                                                                                                                                    10




 1. Floorplan: core_utilization, core_aspect_ratio, core_margins,                      return eda.get_metric(stage="place", metrics=["area", "power",
,→ macro_place_halo, macro_place_channel                                              ,→ "performance"])
 2. CTS: tns_end_percent
 3. Placement: density                                                             # Grid search parameters for each stage
                                                                                   floorplan_params = {
 The user wants to find the best balance between chip area, power consumption,         "core_utilization": {"minmax": [50, 100], "step": 10},
,→ and performance. Therefore, we need to get the "area", "power", and                 "core_aspect_ratio": {"minmax": [0.5, 2.0], "step": 0.5},
,→ "performance" metrics after the final report stage.                                 "core_margins": {"minmax": [1, 5], "step": 1}
                                                                                   }
 Here is the step-by-step analysis of how to complete this task:                   cts_params = {
 1. Initialize the EDA tool using the "setup" function with the design name            "tns_end_percent": {"minmax": [10, 100], "step": 10}
,→ "how" and the platform "gf180".                                                 }
 2. Run logic synthesis using the "run_synthesis" function.                        placement_params = {
 3. Perform floorplan using the "floorplan" function, iterating over the values        "density": {"minmax": [0.1, 1.0], "step": 0.1}
,→ of the parameters to be tuned.                                                  }
 4. Perform placement using the "placement" function, iterating over the values
,→ of the parameters to be tuned.                                                  # Perform grid search
 5. Perform CTS using the "cts" function, iterating over the values of the         tune(floorplan_func, floorplan_params)
,→ parameters to be tuned.                                                         tune(cts_func, cts_params)
 6. Perform global routing using the "global_route" function.                      tune(placement_func, placement_params)
 7. Perform detailed routing using the "detail_route" function.
 8. Perform density fill using the "density_fill" function.
 9. Generate the final report using the "final_report" function.
 10. Get the "area", "power", and "performance" metrics using the "get_metric"
,→ function.
                                                                                  Task 2: Parameter Tuning Process. The next task requires the
                                                                                  model to provide a parameter-tuning solution. Examining the
    S CRIPT G ENERATION                                                           LLM’s use of the tune function also evaluates its ability to sug-
# Define the parameter space for the grid search                                  gest an appropriate parameter space. Without sufficient priors,
floorplan_parameters = [
    {"core_utilization": 70, "core_aspect_ratio": 1, "core_margins": 10,          LLMs cannot determine an exact parameter range. However,
   ,→ "macro_place_halo": 5, "macro_place_channel": 10},
    {"core_utilization": 80, "core_aspect_ratio": 1.2, "core_margins": 15,        we can see that both AutoMage2 and GPT-4 can generate a
   ,→ "macro_place_halo": 10, "macro_place_channel": 15},
    {"core_utilization": 90, "core_aspect_ratio": 1.5, "core_margins": 20,        complex search space, endowing the parameter tuning strategy
   ,→ "macro_place_halo": 15, "macro_place_channel": 20},
]                                                                                 with greater potential. However, GPT-4 misses an important
placement_parameters = [
    {"density": 0.6},                                                             intermediate ”synthesis” step in task decomposition, demon-
    {"density": 0.7},
    {"density": 0.8},                                                             strating weaker comprehension of the task decomposition. In
]
cts_parameters = [                                                                this aspect, AutoMage2 excels.
    {"tns_end_percent": 20},
    {"tns_end_percent": 30},
    {"tns_end_percent": 40},
                                                                                  U SER : What are the tunable parameters in floorplan? Give me
]                                                                                 an example of tuning them for ”router” design on the platform
# Perform the grid search
best_parameters = {"floorplan": None, "placement": None, "cts": None}
                                                                                  sky130 to get the ppa results. Try to provide a larger search
best_performance = float("inf")
for floorplan_parameter in floorplan_parameters:
                                                                                  space for parameter tuning.
    for placement_parameter in placement_parameters:
        for cts_parameter in cts_parameters:
            eda = chateda()
            eda.setup(design_name="how", platform="gf180")
                                                                                  AUTO M AGE 2:
            eda.run_synthesis()
            eda.floorplan(
                core_utilization=floorplan_parameter["core_utilization"],              TASK D ECOMPOSITION
                core_aspect_ratio=floorplan_parameter["core_aspect_ratio"],
                core_margins=floorplan_parameter["core_margins"],                  To complete this task, we need to first identify the tunable parameters in the
                macro_place_halo=floorplan_parameter["macro_place_halo"],         ,→ floorplan function. According to the chateda API documentation, the
                macro_place_channel=floorplan_parameter["macro_place_channel"]    ,→ tunable parameters in the floorplan function are core_utilization,
            )                                                                     ,→ core_aspect_ratio, core_margins, macro_place_halo, and
            eda.placement(density=placement_parameter["density"])                 ,→ macro_place_channel.
            eda.cts(tns_end_percent=cts_parameter["tns_end_percent"])
            eda.global_route()                                                     Next, we need to write a function that takes these parameters as inputs, runs
            eda.detail_route()                                                    ,→ the flow using these parameters and returns the ppa results.
            eda.density_fill()
            eda.final_report()                                                     Finally, we can use the tune function to tune these parameters. We need to
            performance = eda.get_metric("final", ["area", "power",               ,→ provide a dictionary where the keys are the parameter names and the values
           ,→ "performance"])                                                     ,→ are dictionaries containing the ranges and step sizes for these
            if performance < best_performance:                                    ,→ parameters.
                best_performance = performance
                best_parameters["floorplan"] = floorplan_parameter
                best_parameters["placement"] = placement_parameter
                best_parameters["cts"] = cts_parameter
print(best_parameters)                                                                 S CRIPT G ENERATION
                                                                                   def tune_floorplan(core_utilization, core_aspect_ratio, core_margins,
                                                                                  ,→ macro_place_halo, macro_place_channel):
                                                                                       eda_tool = chateda()
                                                                                       eda_tool.setup(design_name="router", platform="sky130")
GPT-4:                                                                                 eda_tool.run_synthesis()
                                                                                       eda_tool.floorplan(core_utilization=core_utilization,
                                                                                      ,→ core_aspect_ratio=core_aspect_ratio, core_margins=core_margins,
                                                                                      ,→ macro_place_halo=macro_place_halo,
    S CRIPT G ENERATION                                                               ,→ macro_place_channel=macro_place_channel)
                                                                                       eda_tool.placement()
# Instantiate the chateda class                                                        eda_tool.cts()
eda = chateda()                                                                        eda_tool.global_route()
# Setup the EDA tool for the "how" design on "gf180" platform                          eda_tool.detail_route()
eda.setup(design_name="how", platform="gf180")                                         eda_tool.density_fill()
# Define the floorplan function for grid search                                        eda_tool.final_report()
def floorplan_func(core_utilization, core_aspect_ratio, core_margins):                 return eda_tool.get_metric("final", ["power", "performance", "area"])
    eda.floorplan(core_utilization=core_utilization,
   ,→ core_aspect_ratio=core_aspect_ratio, core_margins=core_margins)              tunable_params = {
    return eda.get_metric(stage="floorplan", metrics=["area", "power",                 "core_utilization": {"minmax": [0, 100], "step": 5},
   ,→ "performance"])                                                                  "core_aspect_ratio": {"minmax": [0.5, 2], "step": 0.1},
# Define the CTS function for grid search                                              "core_margins": {"minmax": [5, 20], "step": 1},
def cts_func(tns_end_percent):                                                         "macro_place_halo": {"minmax": [5, 20], "step": 1},
    eda.cts(tns_end_percent=tns_end_percent)                                           "macro_place_channel": {"minmax": [5, 20], "step": 1}
    return eda.get_metric(stage="cts", metrics=["area", "power",                   }
   ,→ "performance"])                                                              tune(tune_floorplan, tunable_params)
# Define the placement function for grid search
def placement_func(density):
    eda.placement(density=density)
                                                                                                                                                                     11



GPT-4:                                                                            1.   Initialize the EDA tool with the design name "datacenter_chip" and platform
                                                                                 ,→     "nangate4".
                                                                                  2.   Run logic synthesis with varying clock periods.
      S CRIPT G ENERATION                                                         3.   Perform floorplanning with different core utilization percentages and core
                                                                                 ,→     aspect ratios.
  # Define the target function for parameter tuning                               4.   Run placement with different densities.
  def target_function(core_utilization, core_aspect_ratio, core_margins,          5.   Run clock tree synthesis with different percentages of timing violations to
 ,→ macro_place_halo, macro_place_channel                                        ,→     resolve.
      # Initialize chateda class                                                  6.   Run global routing and detail routing with default parameters.
      eda_tool = chateda()                                                        7.   Run density fill and final report with default parameters.
      # Setup EDA tool with design_name as "router" and platform as "sky130"      8.   Evaluate the performance metrics for power, performance, and area.
      eda_tool.setup(design_name="router", platform="sky130")
      eda_tool.floorplan(
          core_utilization=core_utilization,
          core_aspect_ratio=core_aspect_ratio,
          core_margins=core_margins,                                                    S CRIPT G ENERATION
          macro_place_halo=macro_place_halo,
          macro_place_channel=macro_place_channel                                 def tune_flow(density, core_utilization, clock_period, tns_end_percent,
      )                                                                          ,→ core_aspect_ratio, core_margins, macro_place_halo, macro_place_channel):
      return eda_tool.get_metric(stage="floorplan", metrics=["power",                 eda = chateda()
     ,→ "performance", "area"])                                                       eda.setup(design_name="datacenter_chip", platform="nangate4")
                                                                                      eda.run_synthesis(clock_period=clock_period)
  # Parameters to be tuned with their respective min, max, and step values            eda.floorplan(core_utilization=core_utilization,
  params = {                                                                         ,→ core_aspect_ratio=core_aspect_ratio, core_margins=core_margins,
      "core_utilization": {"minmax": [50, 100], "step": 5},                          ,→ macro_place_halo=macro_place_halo,
      "core_aspect_ratio": {"minmax": [0.5, 2], "step": 0.1},                        ,→ macro_place_channel=macro_place_channel)
      "core_margins": {"minmax": [1, 5], "step": 1},                                  eda.placement(density=density)
      "macro_place_halo": {"minmax": [1, 10], "step": 1},                             eda.cts(tns_end_percent=tns_end_percent)
      "macro_place_channel": {"minmax": [1, 10], "step": 1}                           eda.global_route()
  }                                                                                   eda.detail_route()
  # Tune parameters                                                                   eda.density_fill()
  tune(target_function, params)                                                       eda.final_report()
                                                                                      return eda.get_metric("final", ["power", "performance", "area"])

                                                                                  params = {
                                                                                      "density": {"minmax": [0.1, 0.9], "step": 0.005},
                                                                                      "core_utilization": {"minmax": [60, 85], "step": 1},
 Task 3: Customized Optimization. Customized Optimization                             "clock_period": {"minmax": [5, 10], "step": 1},
                                                                                      "tns_end_percent": {"minmax": [50, 80], "step": 3},
 requires tuning parameters in the model per the search space                         "core_aspect_ratio": {"minmax": [1, 2], "step": 0.2},
                                                                                      "core_margins": {"minmax": [7, 7], "step": 0},
 specified by the user. This process thoroughly evaluates the                         "macro_place_halo": {"minmax": [8, 8], "step": 0},
                                                                                      "macro_place_channel": {"minmax": [9, 9], "step": 0}
 model’s understanding of the tune function and the significance                  }
                                                                                  tune(tune_flow, params)
 of each API interface parameter. It is clear that AutoMage2
 appropriately grasps the need for user-customized parameter
 tuning, while GPT-4 exhibits a deficient understanding of key
 EDA tool arguments. This demonstrates AutoMage2’s more                          GPT-4:
 sophisticated comprehension of the EDA tools’ interface and
 the logic of parameter tuning compared to GPT-4.                                       S CRIPT G ENERATION
 U SER : We are going to develop the new ASIC project “dat-                       def run_eda_flow(density, core_util, clock_period, tns_end_percent,
                                                                                 ,→ core_aspect_ratio, core_margin, macro_halo, macro_channel):
 acenter chip” for server applications on the “nangate4” plat-                        # Setup the design
                                                                                      tool.setup(design_name="datacenter_chip", platform="nangate4")
 form, I want to create an automated script that will tune the                        # Run synthesis with the given clock period
                                                                                      tool.run_synthesis(clock_period=clock_period)
 EDA process, while optimizing for minimal power usage and                            # Perform floorplanning with the specified parameters
                                                                                      tool.floorplan(core_utilization=core_util,
                                                                                     ,→ core_aspect_ratio=core_aspect_ratio, core_margins=core_margin,
 maintaining acceptable performance and area. Here is my plan:                       ,→ macro_place_halo=macro_halo, macro_place_channel=macro_channel)
                                                                                      # Run placement with the specified density
1) Density level ranges from 0.1 to 0.9 with increments of                            tool.placement(density=density)
                                                                                      # Execute CTS to address the specified percentage of timing violations
    0.005;                                                                            tool.cts(tns_end_percent=tns_end_percent)
                                                                                      # Continue with the default parameters for the subsequent EDA steps
2) The search space of core utilization percentages is between                        tool.global_route()
                                                                                      tool.detail_route()
    60% and 85%;                                                                      tool.density_fill()
                                                                                      tool.final_report()
3) Clock periods are ranging from 5 to 10 in steps of 1;                              # Retrieve the power, performance, and area metrics after the EDA flow
                                                                                      power_metric = tool.get_metric(stage="final", metrics=["power"])
4) Resolve 50% to 80% of timing violations, increasing in steps                       performance_metric = tool.get_metric(stage="final",
                                                                                     ,→ metrics=["performance"])
    of 3%;                                                                            area_metric = tool.get_metric(stage="final", metrics=["area"])
                                                                                      # The goal is to minimize power consumption, so we'll use power as our
5) Setting the core aspect ratio play around between 1 and 2                         ,→ primary metric
                                                                                      return power_metric
    with a step of 0.2;
                                                                                  # Parameter space as defined by the user
6) Setting the core margins, macro place halo, and macro place                    param_space = {
                                                                                      "density": {"minmax": [0.1, 0.9], "step": 0.005},
    channel to be 7, 8, 9 separately;                                                 "core_util": {"minmax": [60, 85], "step": 1},
                                                                                      "clock_period": {"minmax": [5, 10], "step": 1},
7) Keeping the rest of the parameters at their default values.                        "tns_end_percent": {"minmax": [50, 80], "step": 3},
                                                                                      "core_aspect_ratio": {"minmax": [1, 2], "step": 0.2},
                                                                                      "core_margin": {"minmax": [7, 9], "step": 1},
                                                                                      "macro_halo": {"minmax": [7, 9], "step": 1},
AUTO M AGE 2:                                                                     }
                                                                                      "macro_channel": {"minmax": [7, 9], "step": 1}

                                                                                  tune(func=run_eda_flow, param=param_space)
      TASK D ECOMPOSITION
  This task requires us to develop a script that automates the EDA process for
 ,→ the "datacenter_chip" project on the "nangate4" platform. The goal is to
 ,→ optimize the design for minimal power usage while maintaining acceptable     Task 4: Clock Period Minimization. During instruction
 ,→ performance and area.
                                                                                 tuning, we guided AutoMage2 on using the API interface
  To achieve this, we need to tune several parameters of the EDA process,
 ,→ including density, core utilization, clock period, percentage of timing      according to our specific needs. However, we did not teach it to
 ,→ violations to resolve, core aspect ratio, core margins, macro place halo,
 ,→ and macro place channel.                                                     modify the parameters using self-feedback from the evaluation
  Here is a step-by-step analysis of how to complete the task:                   results. In this test case, we examined how the LLM adjusts
                                                                                 the API arguments through self-feedback to achieve the user-
                                                                                                                                                                 12



requested evaluation outcomes. Intriguingly, both AutoMage2                    AutoMage2 and GPT-4 can accurately identify and fix errors,
and GPT-4 successfully completed the provided test case.                       demonstrating AutoMage2’s self-correction skills and profound
U SER : Try to find out the smallest valid clock period for the                understanding of utilizing EDA tools.
design “leon” on “asap7” platform. Note that a clock period is                 U SER : Can you help me check whether the provided script can
valid only if the “wns” metric at the final stage is non negative.             complete the task and provide the reason?
                                                                               Here is the task:
AUTO M AGE 2:                                                                  I want to optimize performance at the routing stage for my
                                                                               design asjdk on nangate45.
       TASK D ECOMPOSITION                                                     Here is the script:
 To   achieve this, we can follow these steps:
 1.   Set up the EDA tool with the design name "leon" and platform "asap7".    def tune_performance():
 2.   Run logic synthesis with the smallest possible clock period (default         params = {"tns_end_percent": {"minmax": [0, 100], "step": 5}}
,→     value).                                                                     def optimize(tns_end_percent):
 3.   Perform floorplanning, placement, CTS, global routing, detail routing,           eda = chateda()
,→     density fill, and final report.                                                 eda.setup(design_name="asjdk", platform="nangate45")
 4.   Get the "wns" metric after the final stage.                                      eda.run_synthesis()
 5.   If the "wns" metric is non-negative, return the current clock period.            eda.cts(tns_end_percent=tns_end_percent)
,→     Otherwise, increase the clock period and repeat the process.                    eda.global_route()
                                                                                       eda.detail_route()
                                                                                       return eda.get_metric(stage="route", metrics=["performance"])
                                                                                   best_params = tune(optimize, params)
                                                                                   print(best_params)
       S CRIPT G ENERATION                                                     tune_performance()


 def find_smallest_valid_clock_period(clock_period):
     # Create an instance of the chateda class                                 If the script can’t complete the task, please provide a script that
     eda_tool = chateda()
     # Setup the design                                                        can complete the task.
     eda_tool.setup(design_name="leon", platform="asap7")
     # Run logic synthesis
     eda_tool.run_synthesis(clock_period=clock_period)
     # Run floorplanning                                                       AUTO M AGE 2:
     eda_tool.floorplan()
     # Run placement
     eda_tool.placement()
     # Run CTS
                                                                                     TASK D ECOMPOSITION
     eda_tool.cts()
     # Run global routing                                                        The task is to optimize the performance at the routing stage for the design
     eda_tool.global_route()                                                    ,→ "asjdk" on the "nangate45" platform. The script provided is almost
     # Run detail routing                                                       ,→ correct, but it lacks some necessary steps.
     eda_tool.detail_route()
     # Run density fill                                                          Here are the steps to complete the task:
     eda_tool.density_fill()                                                     1. Set up the EDA tool with the design name "asjdk" and the platform
     # Run final report                                                         ,→ "nangate45".
     eda_tool.final_report()                                                     2. Run logic synthesis.
     # Get the "wns" metric                                                      3. Perform floorplanning.
     wns = eda_tool.get_metric("final", ["wns"])                                 4. Run placement.
     if wns >= 0:                                                                5. Perform clock tree synthesis (CTS) with different percentages of violating
         return clock_period                                                    ,→ paths to fix.
     else:                                                                       6. Run global routing.
         return find_smallest_valid_clock_period(clock_period+1)                 7. Run detail routing.
 # Call the function to find the smallest valid clock period                     8. Get the performance metric after detail routing.
 smallest_valid_clock_period = find_smallest_valid_clock_period(1)               9. Tune the parameter "tns_end_percent" to find the value that gives the best
 print(smallest_valid_clock_period)                                             ,→ performance.




                                                                                     S CRIPT G ENERATION
GPT-4:                                                                          def tune_performance():
                                                                                    params = {"tns_end_percent": {"minmax": [0, 100], "step": 5}}
                                                                                    def optimize(tns_end_percent):
       S CRIPT G ENERATION                                                              eda = chateda()
                                                                                        eda.setup(design_name="asjdk", platform="nangate45")
 # Starting from an assumed clock period. This might need tweaking based on             eda.run_synthesis()
,→ prior knowledge of the design and platform.                                          eda.floorplan()
 clock_period = 1.0 # Initial assumption in nanoseconds (ns)                            eda.placement()
 while True:                                                                            eda.cts(tns_end_percent=tns_end_percent)
     # Initializing the chateda class                                                   eda.global_route()
     eda_tool = chateda()                                                               eda.detail_route()
     # Setup for the 'leon' design on 'asap7' platform                                  return eda.get_metric(stage="route", metrics=["performance"])
     eda_tool.setup(design_name="leon", platform="asap7")                           best_params = tune(optimize, params)
     # Running the synthesis with the current clock_period                          print(best_params)
     eda_tool.run_synthesis(clock_period=clock_period)                          tune_performance()
     # Following the EDA flow
     eda_tool.floorplan()
     eda_tool.placement()
     eda_tool.cts()
     eda_tool.global_route()
     eda_tool.detail_route()
     eda_tool.density_fill()
                                                                               GPT-4:
     eda_tool.final_report()
     # Fetching the WNS metric at the final stage
     wns = eda_tool.get_metric(stage="final", metrics=["wns"])                       S CRIPT G ENERATION
     # If WNS is non-negative, then the current clock_period is valid
     if wns >= 0:
                                                                                def tune_performance():
         break
                                                                                    params = {"tns_end_percent": {"minmax": [0, 100], "step": 5}}
     else:
                                                                                    def optimize(tns_end_percent):
         clock_period += 0.1 # Incrementing the clock period by 0.1ns. This
                                                                                        eda = chateda()
        ,→ step can also be tweaked.
                                                                                        eda.setup(design_name="asjdk", platform="nangate45")
 print(f"The smallest valid clock period for the design 'leon' on 'asap7'
                                                                                        eda.run_synthesis()
,→ platform is {clock_period} ns.")
                                                                                        eda.floorplan() # Adding the floorplan step.
                                                                                        eda.placement() # Adding the placement step.
                                                                                        eda.cts(tns_end_percent=tns_end_percent)
                                                                                        eda.global_route()
                                                                                        eda.detail_route()
Task 5: Self Correct. This task evaluates the model’s ability                           return eda.get_metric(stage="route", metrics=["performance"])
                                                                                    best_params = tune(optimize, params)
to self-correct mistakes, while also testing its capacity to                        print(best_params)
                                                                                tune_performance()
comprehend EDA tools from the opposite perspective. Both
                                                                                                                                                  13



                      VII. D ISCUSSION                              We hope this work could inspire next-generation EDA tool
                                                                    evolution.
Our proposed ChatEDA and its controllers, AutoMage models,
achieve significant performance in the automation of the EDA                                       R EFERENCES
flow. Despite the advancements, they face several limitations.
                                                                     [1] T. Ajayi and D. Blaauw, “OpenROAD: Toward a self-driving, open-source
Firstly, ChatEDA’s design lacks universal applicability to di-           digital layout implementation tool chain,” in Proceedings of Government
verse API documents. This constraint necessitates the creation           Microcircuit Applications and Critical Technology Conference, 2019.
of specialized API documentation for different EDA tools,            [2] “iEDA: An Open-Source Intelligent Physical Implementation Toolkit and
                                                                         Library, author=Li, Xingquan and others,” in International Symposium of
thereby limiting ChatEDA’s scalability and broader application           Electronics Design Automation (ISEDA), 2023.
in the EDA industry. Moreover, the unique API structure of           [3] P. Chen, D. A. Kirkpatrick, and K. Keutzer, “Scripting for eda tools: a case
each EDA tool requires a bespoke approach, impeding the                  study,” in Proceedings of the IEEE 2001. 2nd International Symposium
                                                                         on Quality Electronic Design. IEEE, 2001, pp. 87–93.
seamless integration of ChatEDA across various platforms             [4] T. Brown, B. Mann, N. Ryder, M. Subbiah, J. D. Kaplan, P. Dhariwal,
and tools. Secondly, although AutoMage models demonstrate                A. Neelakantan, P. Shyam, G. Sastry, A. Askell et al., “Language models
enhanced capability in EDA flow automation, they encompass               are few-shot learners,” in Proc. NIPS, 2020.
                                                                     [5] OpenAI, “GPT-4 Technical Report,” 2023.
a substantial number of model parameters. This complexity            [6] T. Anthropic, “Claude,” https://www.anthropic.com/, 2023.
results in a significant slowdown during a single decoding step      [7] H. Touvron, T. Lavril, G. Izacard, X. Martinet, M.-A. Lachaux, T. Lacroix,
in these larger models. Thirdly, we haven’t provided some                B. Rozière, N. Goyal, E. Hambro, F. Azhar et al., “Llama: Open and
                                                                         efficient foundation language models,” arXiv preprint, 2023.
basic evaluation flow or (semi-)automated scoring tools to           [8] H. Touvron, L. Martin, K. Stone, P. Albert, A. Almahairi, Y. Babaei,
support comparisons using the ChatEDA-Bench, which cannot                N. Bashlykov, S. Batra, P. Bhargava, S. Bhosale et al., “Llama 2: Open
enable a straightforward evaluation of different solutions from          Foundation and Fine-Tuned Chat Models,” arXiv preprint, 2023.
                                                                     [9] J. Wei, Y. Tay, R. Bommasani, C. Raffel, B. Zoph, S. Borgeaud,
others. Nevertheless, the assessment of LLM outputs presents             D. Yogatama, M. Bosma, D. Zhou, D. Metzler, E. H. Chi, T. Hashimoto,
challenges due to the vast output space, the subjective nature of        O. Vinyals, P. Liang, J. Dean, and W. Fedus, “Emergent Abilities of Large
relevance and coherence judgments, and considerations regard-            Language Models,” Journal of Machine Learning Research, 2022.
                                                                    [10] Y. Wang, Y. Kordi, S. Mishra, A. Liu, N. A. Smith, D. Khashabi, and
ing human readability. Therefore, we have chosen to leverage             H. Hajishirzi, “Self-instruct: Aligning language model with self generated
human-centric evaluation with the input and insights of multiple         instructions,” arXiv preprint, 2022.
expert judges, aiming to comprehensively evaluate not only the      [11] J. Cui, Z. Li, Y. Yan, B. Chen, and L. Yuan, “ChatLaw: Open-Source
                                                                         Legal Large Language Model with Integrated External Knowledge Bases,”
efficacy of task decomposition but also the quality of script            arXiv preprint, 2023.
generation within the context of LLM outputs. We hope that the      [12] Y. Li, Z. Li, K. Zhang, R. Dan, S. Jiang, and Y. Zhang, “ChatDoctor: A
introduction of a benchmark comprising tasks remains highly              Medical Chat Model Fine-Tuned on a Large Language Model Meta-AI
                                                                         (LLaMA) Using Medical Domain Knowledge,” Cureus, 2023.
beneficial for model debugging and comparative analysis of          [13] J. Wei, M. Bosma, V. Zhao, K. Guu, A. W. Yu, B. Lester, N. Du, A. M.
methodologies.                                                           Dai, and Q. V. Le, “Finetuned Language Models are Zero-Shot Learners,”
   These limitations highlight the imperative for ongoing ef-            in Proc. ICLR, 2022.
                                                                    [14] L. Zheng, W.-L. Chiang, Y. Sheng, S. Zhuang, Z. Wu, Y. Zhuang, Z. Lin,
forts to augment the versatility and efficiency of ChatEDA.              Z. Li, D. Li, E. Xing et al., “Judging LLM-as-a-judge with MT-Bench
Firstly, optimizing the decoding strategy of LLMs to im-                 and Chatbot Arena,” arXiv preprint, 2023.
prove their decoding speed would significantly enhance the          [15] T. Dettmers, A. Pagnoni, A. Holtzman, and L. Zettlemoyer, “QLoRA:
                                                                         Efficient finetuning of quantized LLMs,” arXiv preprint, 2023.
usability and effectiveness of ChatEDA. Moreover, a critical        [16] S. Mukherjee, A. Mitra, G. Jawahar, S. Agarwal, H. Palangi, and
goal is to substantially enhance the generalization capacity             A. Awadallah, “Orca: Progressive learning from complex explanation
of LLMs, enabling them to adeptly manage unfamiliar EDA                  traces of GPT-4,” arXiv preprint, 2023.
                                                                    [17] T. Schick, J. Dwivedi-Yu, R. Dessı̀, R. Raileanu, M. Lomeli, L. Zettle-
tool documentation in zero-shot or few-shot scenarios. Such a            moyer, N. Cancedda, and T. Scialom, “Toolformer: Language models can
breakthrough would significantly broaden the scope of LLMs               teach themselves to use tools,” arXiv preprint, 2023.
in various EDA contexts, representing a major stride in AI-         [18] T. Auto-GPT, “Auto-GPT: An Autonomous GPT-4 Experiment,” https:
                                                                         //github.com/Significant-Gravitas/Auto-GPT, 2023.
powered EDA tool integration and leading to more adaptable          [19] T. BabyAGI, “BabyAGI,” https://github.com/yoheinakajima/babyagi,
and versatile design automation solutions. Additionally, we plan         2023.
to advance the multi-turn dialogue capabilities of our system.      [20] S. G. Patil, T. Zhang, X. Wang, and J. E. Gonzalez, “Gorilla: Large
                                                                         language model connected with massive APIs,” arXiv preprint, 2023.
This enhancement will facilitate dynamic interactions based         [21] J. D. M.-W. C. Kenton and L. K. Toutanova, “BERT: Pre-training
on user feedback, allowing the system to amend and rectify               of Deep Bidirectional Transformers for Language Understanding,” in
potential errors in responses.                                           Proc. NAACL, 2019.
                                                                    [22] Z. Yang, Z. Dai, Y. Yang, J. Carbonell, R. R. Salakhutdinov, and
                                                                         Q. V. Le, “XLNet: Generalized autoregressive pretraining for language
                     VIII. C ONCLUSION                                   understanding,” in Proc. NIPS, 2019.
                                                                    [23] Y. Tay, M. Dehghani, V. Q. Tran, X. Garcia, J. Wei, X. Wang, H. W.
Interfacing EDA tools is essential for unleashing circuit design         Chung, D. Bahri, T. Schuster, S. Zheng et al., “UL2: Unifying language
productivity. In this work, we propose an LLM-powered au-                learning paradigms,” in Proc. ICLR, 2022.
tonomous agent for EDA, which enables a conversational inter-       [24] A. Radford, K. Narasimhan, T. Salimans, I. Sutskever et al., “Improving
                                                                         language understanding by generative pre-training,” 2018.
face for designers to interact with the design flow. Technically,   [25] A. Chowdhery, S. Narang, J. Devlin, M. Bosma, G. Mishra, A. Roberts,
ChatEDA integrates a fine-tuned AutoMage, which orchestrates             P. Barham, H. W. Chung, C. Sutton, S. Gehrmann et al., “Palm: Scaling
the design flow through task decomposition, script generation,           language modeling with pathways,” arXiv preprint, 2022.
                                                                    [26] R. Anil, A. M. Dai, O. Firat, M. Johnson, D. Lepikhin, A. Passos,
and task execution. ChatEDA handles various user requirements            S. Shakeri, E. Taropa, P. Bailey, Z. Chen et al., “Palm 2 technical report,”
well, outperforming other LLM models like GPT-4 and so on.               arXiv preprint, 2023.
                                                                                  14



[27] E. J. Hu, P. Wallis, Z. Allen-Zhu, Y. Li, S. Wang, L. Wang, W. Chen
     et al., “LoRA: Low-Rank Adaptation of Large Language Models,” in
     Proc. ICLR, 2021.
[28] A. Aghajanyan, S. Gupta, and L. Zettlemoyer, “Intrinsic Dimension-
     ality Explains the Effectiveness of Language Model Fine-Tuning,” in
     Proc. ACL, 2021.
[29] S. Min, M. Lewis, L. Zettlemoyer, and H. Hajishirzi, “MetaICL: Learning
     to learn in context,” in Proc. ACL, 2022.
[30] S. M. Xie, A. Raghunathan, P. Liang, and T. Ma, “An explanation of
     in-context learning as implicit bayesian inference,” in Proc. ICLR, 2021.
[31] Q. Dong, L. Li, D. Dai, C. Zheng, Z. Wu, B. Chang, X. Sun, J. Xu, and
     Z. Sui, “A survey for in-context learning,” arXiv preprint, 2022.
[32] E. Frantar, S. Ashkboos, T. Hoefler, and D. Alistarh, “GPTQ: Accurate
     post-training quantization for generative pre-trained transformers,” arXiv
     preprint, 2022.
[33] B. Roziere, J. Gehring, F. Gloeckle, S. Sootla, I. Gat, X. E. Tan, Y. Adi,
     J. Liu, T. Remez, J. Rapin et al., “Code llama: Open foundation models
     for code,” arXiv preprint, 2023.
[34] L. Ouyang, J. Wu, X. Jiang, D. Almeida, C. Wainwright, P. Mishkin,
     C. Zhang, S. Agarwal, K. Slama, A. Ray et al., “Training language models
     to follow instructions with human feedback,” in Proc. NIPS, 2022.
[35] R. Taori, I. Gulrajani, T. Zhang, Y. Dubois, X. Li, C. Guestrin, P. Liang,
     and T. B. Hashimoto, “Stanford Alpaca: An Instruction-following LLaMA
     model,” https://github.com/tatsu-lab/stanford alpaca, 2023.
[36] T. Dettmers, M. Lewis, S. Shleifer, and L. Zettlemoyer, “8-bit optimizers
     via block-wise quantization,” in Proc. ICLR, 2021.
[37] T. Dettmers and L. Zettlemoyer, “The case for 4-bit precision: k-bit
     inference scaling laws,” in Proc. ICML, 2023.
[38] Z. Luo, C. Xu, P. Zhao, Q. Sun, X. Geng, W. Hu, C. Tao, J. Ma, Q. Lin,
     and D. Jiang, “WizardCoder: Empowering Code Large Language Models
     with Evol-Instruct,” arXiv preprint, 2023.
[39] J. Wei, X. Wang, D. Schuurmans, M. Bosma, F. Xia, E. H. Chi, Q. V. Le,
     D. Zhou et al., “Chain-of-Thought Prompting Elicits Reasoning in Large
     Language Models,” in Proc. NIPS, 2022.
[40] H. Su, J. Kasai, Y. Wang, Y. Hu, M. Ostendorf, W.-t. Yih, N. A. Smith,
     L. Zettlemoyer, T. Yu et al., “One embedder, any task: Instruction-
     finetuned text embeddings,” Proc. ACL, 2023.
[41] T. Kojima, S. S. Gu, M. Reid, Y. Matsuo, and Y. Iwasawa, “Large
     Language Models are Zero-Shot Reasoners,” in Proc. NIPS, 2022.
[42] Z. He, H. Wu, X. Zhang, X. Yao, S. Zheng, H. Zheng, and B. Yu,
     “ChatEDA: A Large Language Model Powered Autonomous Agent for
     EDA,” in Proc. MLCAD, 2023.
