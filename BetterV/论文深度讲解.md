# 论文深度讲解：BetterV

> 论文：BetterV: Controlled Verilog Generation with Discriminative Guidance
>
> arXiv：2402.03375v3（cs.AI，2024-05-02 更新）· 发表场所：ICML 2024
>
> 作者：Zehua Pei（Hui-Ling Zhen、Mingxuan Yuan、Yu Huang、Bei Yu）
>
> 单位：The Chinese University of Hong Kong（CUHK）+ Noah's Ark Lab, Huawei（Hong Kong SAR）
>
> 通讯作者：Bei Yu \<byu@cse.cuhk.edu.hk\>

---

## 一、一句话定位

BetterV 是首个把"判别器引导的可控文本生成"思想引入电子设计自动化（Electronic Design Automation, EDA）下游优化的 Verilog 生成框架：通过将开源 6.7B/7B 的代码大语言模型（Large Language Model, LLM）在自构造的 Verilog↔C 配对数据上进行领域特定指令微调（domain-specific instruction tuning），并训练一个生成式判别器（generative discriminator），在解码阶段以加权 Bayes 准则引导 LLM 直接产出对综合（AIG (And-Inverter Graph), 与-反相图）节点数更少与形式验证（SAT (Boolean Satisfiability), 布尔可满足性）求解时间更短的 Verilog 实现，从而在 VerilogEval 上以 7B 规模超越 GPT (Generative Pre-trained Transformer) -4。

---

## 二、EDA 阶段映射 ①—⑰

下面把 BetterV 的工作点映射到 17 个典型集成电路（Integrated Circuit, IC）设计 / EDA 阶段：

| 编号 | EDA 阶段 | BetterV 是否介入 | 介入方式 |
| :---: | :--- | :--- | :--- |
| ① | 系统规格（System Specification / 需求分析） | 否 | 仅以自然语言描述为入口（自然语言 → HDL (Hardware Description Language)） |
| ② | 行为级/算法级建模（Algorithm / Behavior） | 否 | 由人类给出算法思路 |
| ③ | 架构设计（Architecture / 微架构） | 否 | 由人类完成模块划分与时序约定 |
| ④ | RTL（Register Transfer Level, 寄存器传输级）设计（HDL Coding / Verilog 编码） | 是（核心） | BetterV 用 instruct-tuned LLM + 判别器 Bayes 引导直接产出 Verilog 模块；用 V2C 工具对齐 C 语言帮助 LLM 学懂 Verilog |
| ⑤ | 功能仿真（Functional Simulation） | 是（评测） | 用 pass@k + 单元测试（VerilogEval）评估 Verilog 功能正确性 |
| ⑥ | 形式验证（Formal Verification / Equivalence Checking） | 是（优化目标） | Section 4.3.2：生成对 SAT 求解更友好的 SystemVerilog（ANSI-C Benchmark + Yosys `sat -verify -seq 100`） |
| ⑦ | 逻辑综合（Logic Synthesis） | 是（优化目标） | Section 4.3.1：把综合后的 AIG（And-Inverter Graph）节点数作为生成式判别器的"期望/非期望"标签 |
| ⑧ | 静态时序分析（STA (Static Timing Analysis)） | 否（间接） | 通过综合后网表节点数变好，间接有利于后续 STA |
| ⑨ | 门级仿真（Gate-Level Simulation） | 否 | 论文未涉及 |
| ⑩ | 布局规划（Floorplanning） | 否 | 论文未涉及 |
| ⑪ | 布局（Placement） | 否 | 论文未涉及 |
| ⑫ | 布线（Routing） | 否 | 论文未涉及 |
| ⑬ | 时钟树综合（CTS (Clock Tree Synthesis)） | 否 | 论文未涉及 |
| ⑭ | 寄生参数提取（PEX (Parasitic Extraction)） | 否 | 论文未涉及 |
| ⑮ | 物理验证（DRC (Design Rule Check) / LVS (Layout Versus Schematic)） | 否 | 论文未涉及 |
| ⑯ | 签核 / 时序签核（Sign-off） | 否 | 论文未涉及 |
| ⑰ | 流片 / 测试 / 封装（Tape-out / Test / Package） | 否 | 论文未涉及 |

BetterV 的实际"主战场"集中在 ④ RTL 编码、⑤ 功能仿真、⑥ 形式验证、⑦ 逻辑综合 这 4 个阶段，且把 ⑥ 与 ⑦ 作为"下游优化驱动生成"的目标函数进入训练闭环。

**覆盖率: 4/17 核心覆盖（④⑤⑥⑦）。**

---

## 三、输入输出

### 3.1 输入

- 自然语言任务描述（problem description）
- Verilog 模块头（module head），包含 `module` 名、port 列表（input/output）、reg/wire 声明
- （消融/分析时）参考实现 `Reference Verilog`，用于：
  - 形式等价性判别（Yosys `eqy`）
  - 语法正确性（Yosys `prep`）
  - 综合后 AIG 节点比较（Yosys `proc; aigmap; stat`）
  - SAT 求解时间比较（Yosys `hierarchy; proc; opt; sat -verify -seq 100 -tempinduct -prove-asserts`）

### 3.2 输出

- 一段完整的 Verilog（或 SystemVerilog）模块实现：
  - 通过 `endmodule` 闭合
  - 通过 Yosys 语法检查（syntactic correctness）
  - 通过单元测试 / 等价性检查（functional correctness）
  - 与参考实现相比，AIG 节点数显著降低（synthesis optimization）
  - 与参考实现相比，SAT 求解时间显著降低（verification runtime optimization）

### 3.3 训练数据来源（流水线输入）

BetterV 以"自动采集 + 自动处理 + 自动增强"的方式构造训练集，不再额外依赖人工标注的问题—答案对。详细的数据源、过滤规则、V2C 翻译与 Definition-Body 拆分等具体步骤见 §4.2 与 §4.3；评测侧的 pass@k 度量在 §3.4 给出。

为与 §4 配合，这里只列出三类数据来源：

- **抓取的数据池**：GitHub 上开源 Verilog / SystemVerilog 仓库（许可证允许修改与分发）。
- **二次处理得到的配对样本**：用 V2C 工具把 Verilog 转写为等价 C 程序；把 Verilog module 拆分为"头（module 头 + IO 声明）"与"body"。
- **EDA 过滤后的增强样本**：用 instruct-tuned LLM 在高温度下补全 module 头，由 EDA 工具筛除语法错误样本。

### 3.4 评测基准与指标

- 数据集：VerilogEval（Liu et al. 2023b），含 machine-generated 与 human-crafted 两个子集
- 指标：pass@k 无偏估计

$$
pass@k := E_{\text{problems}}\left[1 - \frac{\binom{n-c}{k}}{\binom{n}{k}}\right]
$$

其中 $n$ 为每题生成的总样本数，$c$ 为通过单元测试的样本数，$k$ 为从中抽取的样本数；每题生成 $n$ 个样本，$k$ 个中只要存在 1 个通过单元测试即算通过。

### 3.5 主要对照模型

GPT-3.5、GPT-4、CodeLlama-7B-Instruct、DeepSeek-Coder-6.7b-Instruct、CodeQwen1.5-7B-Chat、BetterV-CodeLlama、BetterV-DeepSeek、BetterV-CodeQwen、ChipNeMo、Thakur et al.、VerilogEval（基线）、RTLCoder-Mistral、RTLCoder-DeepSeek。

---

## 四、方法与架构

BetterV 框架由四步相互衔接的步骤组成：① 数据处理（含定义—Body 拆分 + V2C 代码知识迁移）→ ② 域特定 instruct-tuning（Code Knowledge Transfer + Verilog Autocompletion）→ ③ 生成式判别器训练 → ④ Bayes 引导解码。Figure 1 给出了这张端到端流程图，下面用 ASCII 重新勾勒其管道：

```
 ┌────────────────────────────────────────────────────────────────────────────┐
 │  Open-source Verilog (GitHub)                                              │
 │      │  license-filter, autogen-filter, length-filter                      │
 │      ▼                                                                     │
 │  ┌──────────────────────────────────┐   ┌──────────────────────────────┐    │
 │  │ Filtering / Regex extraction    │   │   V2C Tool (Mukherjee 2016)   │    │
 │  │  (modules & functions)          │──▶│   Verilog ───▶ equivalent C   │    │
 │  └──────────────────────────────────┘   └──────────────────────────────┘    │
 │      │                                 Verilog-C pairs                        │
 │      │ module header  │ body                                                     │
 │      ▼                                                                     │
 │  ┌──────────────────────────────────┐                                       │
 │  │ Definition-Body Splitting        │                                       │
 │  └──────────────────────────────────┘                                       │
 │      │                                                                     │
 │      ▼                                                                     │
 │  ┌────────────────────────────────────────────────────────────────────┐     │
 │  │ Customized Dataset = { Verilog-C pairs, Verilog Def-Body pairs }    │     │
 │  └────────────────────────────────────────────────────────────────────┘     │
 │      │                                                                     │
 └──────┼─────────────────────────────────────────────────────────────────────┘
        │
        ▼
 ┌──────────────────────────────────────────────────────────────────────────┐
 │  Pre-trained LLM  (CodeLlama-7B / DeepSeek-6.7B / CodeQwen-7B)            │
 │      │                                                                     │
 │      ├─► Instruct Task A: Code Knowledge Transfer (Verilog ⇆ C)            │
 │      │       输入: Verilog 模块            标签: 等价 C 程序               │
 │      │       输入: C 程序                标签: 等价 Verilog 模块           │
 │      │                                                                     │
 │      ├─► Instruct Task B: Verilog Autocompletion                           │
 │      │       输入: module 定义(头+IO)   标签: 完整 module body             │
 │      │                                                                     │
 │      │   训练目标: 最小化 NLL (Negative Log-Likelihood) (auto-regressive, Equation 2)                │
 │      │   工具: DeepSpeed ZeRO (Zero Redundancy Optimizer) + LoRA (Low-Rank Adaptation, dim=128) + Adam                       │
 │      ▼                                                                     │
 │  ┌────────────────────────────────────────────────────────────────────┐   │
 │  │   Fine-tuned Model        (System Prompt: "experienced Verilog    │   │
 │  │                            engineer")                             │   │
 │  └────────────────────────────────────────────────────────────────────┘   │
 │      │                                                                     │
 │      │   ── Data Augmentation Loop ──                                    │
 │      │   高温度采样补全 module 头  ──▶  EDA tool 语法过滤  ──▶ 加入训练集│
 │      ▼                                                                     │
 └──────────────────────────────────────────────────────────────────────────┘
        │
        │
        ▼
 ┌──────────────────────────────────────────────────────────────────────────┐
 │  Discriminator Training                                                   │
 │                                                                          │
 │  ┌──────────────────┐    ┌──────────────────────────────────────┐         │
 │  │ EDA-Tools        │───▶│  Desired Verilog   (label = c)       │         │
 │  │  (Yosys, eqy,    │    │  Undesired Verilog (label = \bar{c}) │         │
 │  │   SAT, AIG stat) │    └──────────────────────────────────────┘         │
 │  └──────────────────┘                       │                            │
 │                                            ▼                            │
 │  ┌──────────────────────────────────────────────────────────┐            │
 │  │ Generative Discriminator   (TinyLlama / DeepSeek-1.3B)   │            │
 │  │   Hybrid Loss:  L_total = λ L_g + (1-λ) L_d               │            │
 │  └──────────────────────────────────────────────────────────┘            │
 │      │                                                                    │
 └──────┼────────────────────────────────────────────────────────────────────┘
        │
        ▼
 ┌──────────────────────────────────────────────────────────────────────────┐
 │  Guidance Decoding (Inference, Bayes-rule weighted decoding)              │
 │                                                                          │
 │   for t = 1..n:                                                          │
 │       for each candidate token x_t in vocabulary V:                       │
 │           score ∝ p_LLM(x_t | x_<t) · p_θ(c | x_t, x_<t)^w              │
 │       token-filter:                                                       │
 │           rank all x_t by DISCRIMINATOR prob p_θ(c | x_t, x_<t)           │
 │           V_τ = { x_t | p_θ(c | x_t, x_<t) > τ }                         │
 │           V_m = top-m tokens such that                                     │
 │                    Σ_{x_t ∈ V_rank[1:m]} p_w(x_t | x_<t, c)  ≥  ρ        │
 │           V_k   = V_τ ∪ V_m                                                │
 │       sample x_t ~ softmax(score) over V_k                                 │
 │       append x_t to x_<t                                                  │
 │                                                                          │
 │   Desired code = generation directed by LLM × class-conditional disc.     │
 │   Undesired code = generation anti-directed by anti-control code \bar{c}. │
 └──────────────────────────────────────────────────────────────────────────┘
```

### 4.1 框架总览（Section 3.1）

- 第一步：从 GitHub Verilog 出发构造 customized dataset（Section 3.2）
- 第二步：在 customized dataset 上做 domain-specific instruct-tuning（Section 3.3）
- 第三步：基于（可能是增强后的）数据训练生成式判别器（Section 3.5）
- 第四步：在 decoding 阶段用 Bayes rule 加权，引导 LLM 偏向期望方向

### 4.2 数据处理（Section 3.2）

- 参考 Dehaerne 等人的做法抓取 GitHub 上 Verilog / SystemVerilog 仓库
- 强制要求许可证允许修改与分发
- 过滤 auto-generated 文件 / 非宽松许可文件 / 过短 / 过长
- 用正则抽取 module 与 function
- 用 tokenizer 的 token 数过滤超过最大长度的内容（理由：超长内容会被截断，含义残缺）
- 过滤有语法错误的样本

### 4.3 两条处理路线

#### 路线 A：Code Knowledge Transfer（Code⇄Verilog 对齐）

- 用 V2C 工具把 Verilog 翻译成 C
- 翻译结果不超最大 token 数才追加到数据集中
- 这样做的动机：Verilog 语料稀缺，但 C 在 LLM 预训练语料中极为丰富；通过 C⇄Verilog 双向翻译任务，LLM 能"借"已学到的 C 语言知识来理解 Verilog

#### 路线 B：Definition-Body Split（定义—Body 拆分）

- 把每个 module 拆为头部（module 头 + IO 声明）和 body
- 头部当作 instruct-tuning 的输入、body 当作目标输出（Verilog Autocompletion）
- 这样在 instruct-tuning 时不再额外加入自然语言描述，把"实现"本身当作功能性描述
- 最终得到的定制数据 = { Verilog-C pairs } ∪ { Verilog definition-body pairs }

### 4.4 域特定 Instruct-Tuning（Section 3.3）

两类指令：

- Task A: Code Knowledge Transfer
  - Instruction："把下面 Verilog 翻译成 C 程序" → Answer：C 代码
  - Instruction："把下面 C 程序翻译成 Verilog 模块" → Answer：Verilog
- Task B: Verilog Autocompletion
  - Instruction：Verilog module 头（端口列表）→ Answer：完整 module body（含 `endmodule`）

训练目标遵循 auto-regressive 因式分解（Equation 1、2）：

LLM 用参数 $\theta$ 预测序列概率，对数据集 $D$ 最小化负对数似然（NLL, Negative Log-Likelihood）：

$$
L = -\frac{1}{|D|} \sum_{i=1}^{|D|} \frac{1}{n} \sum_{t=1}^{n} \log p_\theta(x_t^i | x_{<t}^i)
$$

其中 $|D|$ 为训练序列总数，$n$ 为每条序列的长度，$x_t^i$ 表示第 $i$ 条序列的第 $t$ 个 token，$x_{<t}^i$ 为其前缀。

论文图 2 给出 d_latch 例子用以示意；其中"Verilog⇆C 对齐"对应左边示例，"Verilog Autocompletion"对应右边示例。

### 4.5 数据增强（Section 3.4）

#### 4.5.1 对 instruct-tuning 的数据增强

- 收集阶段已经得到一批 module 头
- 用 instruct-tuned LLM 在高 temperature 下做 module 补全，得到多样化样本
- 用 EDA 工具做语法正确性过滤
- 把通过过滤的合成样本追加到数据集中（提升鲁棒性、泛化、并强化生成"语法正确 Verilog"的能力）

#### 4.5.2 对判别器训练的数据准备

- 数据标签分两类：
  - 绝对正确/错误（如语法 / 行为等价）→ 直接给 desired / undesired 标签
  - 相对好坏（如综合后网表节点、SAT 求解时间）→ 与参考 Verilog 对比，再给 desired / undesired
- 做法：用 LLM 在 module 头上补全得到多个变体，再用 EDA 工具按属性打标

### 4.6 生成式判别器（Section 3.5）

#### 4.6.1 形式化（CC-LM (Class-Conditional Language Model) 视角）

对于类条件语言模型（class-conditional LM）：

$$
p_\theta(x | c) = \prod_{t=1}^{n} p_\theta(x_t | x_{<t}, c)
$$

- 二元（binary）情形：分别有 control code $c$ 与 anti-control code $\bar{c}$
- 期望 / 非期望两种属性对应到 $c$ 与 $\bar{c}$

#### 4.6.2 Bayes 引导公式

对每个候选 token $x_t$，由 Bayes rule 得到序列归属某类的判别概率：

$$
p_\theta(c_y | x_{1:t}) = \frac{p(c_y)\, p_\theta(x_{1:t} | c_y)^{\alpha / t}}{\sum_{c' \in \{c, \bar{c}\}} p(c')\, p_\theta(x_{1:t} | c')^{\alpha / t}}
$$

其中：
- $c_y \in \{ c, \bar{c} \}$ 为序列标签
- $\alpha$ 是可学习尺度参数
- $p(c) = \mathrm{softmax}$ over learnable bias $b_c$

#### 4.6.3 双目标联合训练

- 生成损失 $L_g$：对每个属性 $c' \in \{ c, \bar{c} \}$ 做生成式预测（即语言建模目标）

$$
L_g = -\frac{1}{|D|} \sum_{i=1}^{|D|} \frac{1}{n} \sum_{t=1}^{n} \log p_\theta(x_t^i | x_{<t}^i, c')
$$

- 判别损失 $L_d$：在给定完整序列时预测类别

$$
L_d = -\frac{1}{|D|} \sum_{i=1}^{|D|} \log p_\theta(c_y^i | x_{1:n}^i)
$$

- 总损失（论文 Equation 7）：

$$
L_{\text{total}} = \lambda\, L_g + (1 - \lambda)\, L_d
$$

其中 $\lambda \in [0, 1]$ 用于在生成 / 判别两个目标间取平衡。

#### 4.6.4 Bayes 加权解码（Equation 8）

在解码时刻，对所有 token $x_t$ 计算加权分数：

$$
p_w(x_t | x_{<t}, c) \propto p_{\text{LLM}}(x_t | x_{<t})\, p_\theta(c | x_t, x_{<t})^w
$$

- 右侧第一项是 LLM 的下一 token 概率
- 右侧第二项是判别器在已经把 $x_t$ 并入上下文后的判别概率的 $w$ 次幂
- $w$ 越大，判别器的引导越强

#### 4.6.5 Token 过滤（Equations 9 与上下文）

为了避免被引导到语法不通顺或判别器高判别概率但语言模型概率极低的 token，论文采用 token-filter 两步走：

- 取累积概率阈值 $\rho$：先把所有候选 token 按 **判别器概率** $p_\theta(c | x_t, x_{<t})$ **排序**得到 $V_{\text{rank}}$，然后取前 $m$ 个使 $p_w$ 的累计质量 $\geq \rho$，得集合 $V_m = V_{\text{rank}}[1:m]$
- 取判别置信度阈值 $\tau$：对所有 $p_\theta(c | x_t, x_{<t}) > \tau$ 的 token 强制保留，记为 $V_\tau$
- 最终候选集合 $V_k = V_\tau \cup V_m$
- 在 $V_k$ 上做 softmax 采样

### 4.7 Figure 3（Bayes 引导示意）

论文 Figure 3 描绘：对 partial prefix "module d_latch"，LLM 给出的 $p_{\text{LLM}}$ 与生成式判别器给出的 $p_\theta(c | x_t, x_{<t})$（图中简写为 $p_{\checkmark}$）通过 Bayes rule 合成最终 token 概率，进而在 $p_{\text{LLM}}$ 与 $p_{\checkmark}$ 两组数值共同作用下选出下一个 token（如左括号、左大括号），被选中的 token 进入下一步前缀继续滚动预测。

---

## 五、关键公式汇总

### 5.1 Auto-regressive 因式分解（Equation 1）

$$
p_\theta(x) = \prod_{t=1}^{n} p_\theta(x_t | x_{<t})
$$

其中 $x = (x_1, \dots, x_n)$ 是长度为 $n$ 的 token 序列，$x_{<t}$ 为 $t$ 之前的前缀。该式是对联合分布 $p_\theta(x)$ 沿 token 顺序作链式分解（Chain Rule）；它使得训练与解码只需对条件概率 $p_\theta(x_t | x_{<t})$ 建模，而无需直接估计完整序列的联合分布。

### 5.2 LLM 训练目标 / NLL（Equation 2）

$$
L = -\frac{1}{|D|} \sum_{i=1}^{|D|} \frac{1}{n} \sum_{t=1}^{n} \log p_\theta(x_t^i | x_{<t}^i)
$$

其中 $|D|$ 为训练序列数，$n$ 为序列长度，求和遍历数据集中所有 token。把式 5.1 取对数后再取负号、并对数据集按样本 / token 做平均，就等价于最大似然估计（MLE）；这就是标准的负对数似然（NLL）训练目标。

### 5.3 Class-Conditional LM 因式分解（Equation 3）

$$
p_\theta(x | c) = \prod_{t=1}^{n} p_\theta(x_t | x_{<t}, c)
$$

其中 $c$ 为控制码（control code），用以指定 desired / undesired 属性。

### 5.4 Bayes 引导的判别概率（Equation 5）

$$
p_\theta(c_y | x_{1:t}) = \frac{p(c_y)\, \prod_{j=1}^{t} \left( p_\theta(x_j | x_{<j}, c_y) \right)^{\alpha/t}}{\sum_{c' \in \{c, \bar{c}\}} p(c')\, \prod_{j=1}^{t} \left( p_\theta(x_j | x_{<j}, c') \right)^{\alpha/t}}
$$

其中 $c_y \in \{c, \bar{c}\}$ 为序列标签，$\alpha$ 为可学习尺度参数，分母对所有候选类做归一化。这是用 Bayes 公式把"已知前缀 $x_{1:t}$ 的情况下序列属于类 $c_y$ 的后验"用"先验 $p(c_y)$ × 类条件似然的 $\alpha/t$ 次幂"表达；类条件似然通过式 5.3 的链式分解得到，$\alpha/t$ 起到了对前缀长度做归一化的作用。

### 5.5 判别器生成损失 $L_g$（Equation 4）

$$
L_g = -\frac{1}{|D|} \sum_{i=1}^{|D|} \frac{1}{n} \sum_{t=1}^{n} \log p_\theta(x_t^i | x_{<t}^i, c')
$$

其中 $c' \in \{c, \bar{c}\}$ 遍历期望与非期望两种属性。

### 5.6 判别器判别损失 $L_d$（Equation 6）

$$
L_d = -\frac{1}{|D|} \sum_{i=1}^{|D|} \log p_\theta(c_y^i | x_{1:n}^i)
$$

其中 $c_y^i$ 为第 $i$ 条序列的真实类标签，$x_{1:n}^i$ 为完整序列。

### 5.7 总损失（Equation 7）

$$
L_{\text{total}} = \lambda\, L_g + (1 - \lambda)\, L_d
$$

其中 $\lambda \in [0, 1]$ 为生成损失与判别损失之间的平衡系数。$L_g$（式 5.5）保证判别器本身仍是一个语言模型（保留生成能力），$L_d$（式 5.6）则在完整序列上训练其判别能力；通过 $\lambda$ 在二者间插值可兼顾两项目标。

### 5.8 Bayes 加权解码（Equation 8）

$$
p_w(x_t | x_{<t}, c) \propto p_{\text{LLM}}(x_t | x_{<t})\, p_\theta(c | x_t, x_{<t})^w
$$

其中 $\propto$ 表示正比于，$w$ 为引导强度超参。该式把 LLM 的下一 token 概率与判别器在把 $x_t$ 并入前缀后的判别概率的 $w$ 次幂相乘；$w$ 越大，引导越强，但过强会牺牲 LLM 流畅度，所以 §5.9 的 token-filter 会进一步剔除判别概率虽高但 LLM 概率极低的 token。

### 5.9 Token-filter 中的累积概率阈值（Equation 9）

$$
\sum_{x_t \in V_{\text{rank}}[1:m]} p_w(x_t | x_{<t}, c) \geq \rho
$$

其中 $V_{\text{rank}}[1:m]$ 是按判别器概率 $p_\theta(c | x_t, x_{<t})$ 排序后的前 $m$ 个 token，$\rho$ 为累积质量阈值。

### 5.10 pass@k 无偏估计（Equation 10）

$$
pass@k := E_{\text{problems}}\left[1 - \frac{\binom{n - c}{k}}{\binom{n}{k}}\right]
$$

其中 $n$ 为每题生成的样本数，$c$ 为通过数，$k$ 为抽取数。直观解释：从 $n$ 个样本中无放回抽 $k$ 个，全不通过的概率是 $\binom{n-c}{k}/\binom{n}{k}$；$1$ 减去它就是"至少一个通过"的无偏估计。

---

## 六、训练与实验

### 6.1 训练配置

- 训练硬件：两卡 NVIDIA Tesla V100S PCIe 32 GB，CUDA 11.4
- 训练框架：DeepSpeed-Chat（Yao et al. 2023） + DeepSpeed ZeRO (Zero Redundancy Optimizer)（Rajbhandari et al. 2020）
- 微调方法：LoRA (Low-Rank Adaptation)（Hu et al. 2021），dim = 128（LLM 与判别器均如此）
- 优化器：Adam（Kingma & Ba 2014），β1 = 0.9，β2 = 0.95
- 学习率策略：cosine learning rate decay（Loshchilov & Hutter 2016）
- 训练超参
  - LLM 微调：4 epoch，初始学习率 9.65e-6，batch size 4
  - 生成式判别器：3 epoch，初始学习率 9.65e-6，batch size 8
- 可学习参数：$\alpha$ 初始化为 1，$b$ 初始化为 0
- 任务敏感超参：$\lambda$、$w$、$\rho$、$\tau$ 在不同下游任务上取值不同（论文按下游任务调优）

### 6.2 主干模型与判别器

| 角色 | 候选骨干 |
| :--- | :--- |
| 生成式 LLM（fine-tuned）| CodeLlama-7B-Instruct（Roziere et al. 2023）、DeepSeek-Coder-6.7b-Instruct（Guo et al. 2024）、CodeQwen1.5-7B-Chat（Bai et al. 2023）|
| 生成式判别器 | TinyLlama（Zhang et al. 2024）、DeepSeek-Coder-1.3b-Instruct（Guo et al. 2024）|

- 论文约定：CodeQwen1.5-7B-Chat 没有可用的更小版本，因此只能 fine-tune，不能配合更深一层的 smaller-size 判别器；因此它在消融中不与判别器耦合使用
- "BetterV" 在 Section 4.3 之后等价于 "BetterV-CodeLlama"；"BetterV-base" 是 instruct-tuned but 未被判别器增强的版本

### 6.3 评测协议

- 数据集：VerilogEval（machine-generated / human-crafted 两个子集）
- 度量：pass@k（k=1, 5, 10），无偏估计
- 采样：每题 n=20，k ∈ {1, 5, 10}
- 下游任务评测时，正确性通过 EDA 工具（Yosys）评估

### 6.4 功能正确性（Section 4.2，Table 1）

> 表中数据为论文 Table 1 复刻

| 模型 | VerilogEval-machine pass@1 | pass@5 | pass@10 | VerilogEval-human pass@1 | pass@5 | pass@10 |
| :--- | ---: | ---: | ---: | ---: | ---: | ---: |
| GPT-3.5 | 46.7 | 69.1 | 74.1 | 26.7 | 45.8 | 51.7 |
| GPT-4 | 60.0 | 70.6 | 73.5 | 43.5 | 55.8 | 58.9 |
| CodeLlama（基线） | 43.1 | 47.1 | 47.7 | 18.2 | 22.7 | 24.3 |
| DeepSeek（基线） | 52.2 | 55.4 | 56.8 | 30.2 | 33.9 | 34.9 |
| CodeQwen（基线） | 46.5 | 54.9 | 56.4 | 22.5 | 26.1 | 28.0 |
| ChipNeMo | 43.4 | - | - | 22.4 | - | - |
| Thakur et al. | 44.0 | 52.6 | 59.2 | 30.3 | 43.9 | 49.6 |
| VerilogEval | 46.2 | 67.3 | 73.7 | 28.8 | 45.9 | 52.3 |
| RTLCoder-Mistral | 62.5 | 72.2 | 76.6 | 36.7 | 45.5 | 49.2 |
| RTLCoder-DeepSeek | 61.2 | 76.5 | 81.8 | 41.6 | 50.1 | 53.4 |
| BetterV-CodeLlama | 64.2 | 75.4 | 79.1 | 40.9 | 50.0 | 53.3 |
| BetterV-DeepSeek | 67.8 | 79.1 | 84.0 | 45.9 | 53.3 | 57.6 |
| BetterV-CodeQwen | 68.1 | 79.4 | 84.5 | 46.1 | 53.7 | 58.2 |

- BetterV-CodeQwen 在 VerilogEval-machine 与 VerilogEval-human 上分别比 GPT-4 高 8.1 与 2.6（pass@1）
- CodeLlama（最低基线）经过 instruct-tuning + 判别器引导后被大幅拉高
- BetterV-DeepSeek / BetterV-CodeQwen 在所有 k 上均位居当时 SOTA (State of the Art)

### 6.5 下游任务 ①—综合 AIG 节点缩减（Section 4.3.1，Table 2）

- 任务：让 Verilog 在综合后比参考有更少的 AIG 节点
- 数据标签规则：综合后网表节点数 < 参考 → desired（$c$）；否则 → undesired（$\bar{c}$）
- 节点统计方式：Yosys 命令 `proc; aigmap; stat`，把 Verilog 转为 AIG
- 提示词模板：把参考 Verilog 改写成与原模块同义但 AIG 节点更少（参见 Figure 4）
- 评估问题：VerilogEval-human 子集中的若干题
- 结果（节点数平均）：论文 Table 2

| 问题 | Ref | BetterV-base | BetterV | Com Base | Com Ref |
| :--- | ---: | ---: | ---: | ---: | ---: |
| ece241 2013 q8 | 657 | 333.5 | 255.3 | 23.44% | 61.14% |
| m2041 q6 | 1370 | 692.7 | 685.6 | 1.03% | 49.95% |
| counter 2bc | 673 | 666.2 | 518.9 | 22.11% | 22.89% |
| review2015 count1k | 487 | 493.4 | 402.6 | 18.44% | 17.33% |
| timer | 498 | 294.3 | 247.3 | 15.97% | 50.34% |
| edgedetect2 | 58 | 189.9 | 47.4 | 75.03% | 18.27% |
| counter1to10 | 325 | 266.3 | 240.3 | 9.76% | 26.06% |
| 2013 q2afsm | 826 | 308.8 | 296.6 | 3.95% | 64.09% |
| dff8p | 50 | 42.3 | 37.8 | 10.63% | 24.4% |
| fsm3comb | 844 | 167.9 | 104.4 | 37.82% | 87.63% |
| rule90 | 6651 | 12435.6 | 4536.9 | 63.52% | 31.79% |
| mux256to1v | 2376 | 2439.6 | 557.2 | 77.16% | 76.54% |
| fsm2 | 389 | 186.53 | 121.9 | 34.65% | 68.66% |
| fsm2s | 396 | 163.7 | 144.1 | 11.97% | 63.61% |
| ece241 2013 q4 | 2222 | 1789.5 | 897.4 | 49.85% | 59.61% |
| conwaylife | 43794 | 547400.3 | 27037.4 | 95.06% | 38.26% |
| count clock | 3187 | 2497.5 | 2222.2 | 11.02% | 30.27% |
| countbcd | 1589 | 932.0 | 849.3 | 8.87% | 46.55% |

- 综合 18 题平均：
  - 较 Reference：节点数下降 46.52%
  - 较 BetterV-base：节点数下降 31.68%
- 这表示 LLM 直接参与到 EDA 的 PPA (Power Performance Area) 优化中，是论文最显著的工程信号

### 6.6 下游任务 ②—SAT 形式验证时间缩减（Section 4.3.2，Table 3）

- 任务：把 Verilog 改写成与参考功能相同、但对 SAT 求解更友好的版本
- 数据标签规则：求解时间 < 参考 → desired；否则 undesired
- 评估基准：ANSI-C Benchmark（含 `assert` 的 SystemVerilog 安全断言）
  - 论文声明 VerilogEval 不含带断言的 SystemVerilog，因此改用 ANSI-C benchmarks（Mukherjee et al. 2015）
  - 对 "Rotate" 题，论文额外 uncomment 了所有断言以提升难度
- Yosys 命令：`hierarchy; proc; opt; sat -verify -seq 100 -tempinduct -prove-asserts`，时间步 100
- 提示词模板：在 Figure 4 的基础上，把"less AIG nodes"换成"less SAT solving time"
- 结果（论文 Table 3）：

| 设计 | Ref (s) | BetterV-base (s) | BetterV (s) | Com Base | Com Ref |
| :--- | ---: | ---: | ---: | ---: | ---: |
| b03 | 1.233 | 1.252 | 0.857 | 31.54% | 30.49% |
| b06 | 0.099 | 0.083 | 0.078 | 6.02% | 21.21% |
| Spinner | 1.577 | 1.343 | 1.064 | 20.77% | 32.53% |
| traffic light example | 0.583 | 0.497 | 0.480 | 3.42% | 17.67% |
| Rotate | 1.153 | 1.126 | 1.034 | 8.17% | 10.32% |

- 平均相对 Reference 节省 22.45% 验证时间，相对 BetterV-base 节省 13.99%

### 6.7 消融：判别器对功能正确性的影响（Section 4.4.1，Table 4）

- 等价性检查工具：Yosys `eqy`
- 评估集：VerilogEval-human

| 模型 | pass@1 | pass@5 | pass@10 |
| :--- | ---: | ---: | ---: |
| CodeLlama（基线） | 18.2 | 22.7 | 24.3 |
| CodeLlama + Dis | 20.3 | 24.1 | 24.7 |
| BetterV-CodeLlama-base | 40.0 | 49.5 | 53.0 |
| BetterV-CodeLlama | 40.9 | 50.0 | 53.3 |

- 判别器对原始 CodeLlama 与 BetterV-CodeLlama-base 都有提升，证明它是 Plug-and-Play 的"加成器"，前提是 LLM 与判别器有同样的词表大小

### 6.8 消融：判别器对语法正确性的影响（Section 4.4.1，Table 5）

- 语法标签方式：用 Yosys `prep`（通用综合脚本）判断能否被编译
- 评估集：VerilogEval-human

| 模型 | pass@1 | pass@5 | pass@10 |
| :--- | ---: | ---: | ---: |
| CodeLlama | 41.5 | 50.8 | 53.8 |
| CodeLlama + Dis | 49.1 | 58.4 | 61.1 |
| BetterV-CodeLlama-base | 82.6 | 97.6 | 99.2 |
| BetterV-CodeLlama | 87.1 | 98.2 | 99.3 |

- BetterV-CodeLlama 在 pass@10 已 >99
- 判别器对 pass@1 在 CodeLlama / BetterV-CodeLlama-base 上分别提升 7.6 / 4.5

### 6.9 提示词与指令模板（论文 Figure 2、Figure 4）

- System Prompt 固定："You are an experienced Verilog engineer."
- Task A（Code Knowledge Transfer）："You are supposed to translate the following Verilog into C program."
- Task B（Verilog Autocompletion）："Below is a definition of a Verilog module. And you are supposed to complete the Verilog program."
- 下游任务改写提示（综合 / SAT）："Below is a Verilog module. And you are supposed to rewrite this Verilog module with the same definition but less And-Inverter-Graph nodes after synthesis."（AIG 节点改写）；SAT 改写仅换描述词 "less Boolean Satisfiability (SAT) solving time"
- 注意：instruct-tuning 阶段不再附加自然语言描述；以"实现"本身（C 程序或 module body）作为功能描述

---

## 七、创新点（按贡献位次）

1. **首次把判别器引导的可控文本生成范式应用到工程优化领域**
   - 不是控制语义 / 风格等文本属性，而是直接优化 EDA 下游目标
   - 为 EDA 与可控生成两条线都打开新方向

2. **首个"下游任务驱动"的 Verilog 生成方法**
   - 用 EDA 工具（AIG、SAT、Yosys 等）作为下游任务 oracle
   - 把"如何实现 Verilog"翻译为"如何优化 Verilog 实现"
   - 训练效率和工业可用性明显提升

3. **以 6.7B/7B 规模微调 LLM + 判别器，无需 prompt engineering 即超越 GPT-4**
   - VerilogEval 上 BetterV-CodeQwen 在 pass@1 上比 GPT-4 高 8.1（machine）/ 2.6（human）
   - 表明 instruction-tuning + discriminative guidance 的算力—性能比优于商业大模型基线

4. **可在 LLM 与判别器训练间复用的数据增强管线（在论文实验的 Verilog 风格范围内）**
   - 数据稀缺时通过 instruct-tuned LLM 在高温度下补全 module 头
   - EDA 工具做语法过滤，保证加入数据集的样本都是"语法正确 Verilog"
   - 增强样本可同时用于 instruct-tuning 和判别器训练
   - 适用范围仍限于论文评测过的常见 Verilog 编码风格，不应外推为"任意 Verilog 规格"

5. **Code Knowledge Transfer（Verilog ⇆ C）的代码知识迁移设计**
   - 用 V2C 工具（Mukherjee et al. 2016）让 LLM 利用已有的 C 语言知识
   - 缓解 Verilog 语料稀缺对 LLM 学习的限制

6. **类条件生成式判别器 + Bayes-rule 加权解码的统一抽象**
   - 训练损失：$L_g + L_d$ 的加权和（Equation 7）
   - 解码公式：$p_w \propto p_{\text{LLM}} \cdot p_\theta(c | x_t, x_{<t})^w$（Equation 8）
   - Token-filter（$\rho$ 累积概率 + $\tau$ 判别置信度）保证引导不破坏 LLM 流畅度

---

## 八、EDA / LLM 缩写表

| 缩写 | 全称 | 含义 |
| :--- | :--- | :--- |
| EDA | Electronic Design Automation | 电子设计自动化软件集合 |
| HDL | Hardware Description Language | 硬件描述语言（Verilog、VHDL 等） |
| RTL | Register Transfer Level | 寄存器传输级，通常对应 Verilog 编码层级 |
| PPA | Power, Performance, Area | 衡量电路设计的三个核心指标 |
| AIG | And-Inverter Graph | 与-反相图，一种以两输入 AND + 反相器节点表示的布尔网络 |
| SAT | Boolean Satisfiability | 布尔可满足性问题（判定某布尔公式是否存在满足赋值） |
| V2C | Verilog-to-C Translator | Verilog → C 翻译工具（Mukherjee et al. 2016, TACAS） |
| LLM | Large Language Model | 大语言模型 |
| LoRA | Low-Rank Adaptation | 低秩适配微调方法（Hu et al. 2021） |
| ZeRO | Zero Redundancy Optimizer | DeepSpeed 的零冗余优化器（Rajbhandari 2020） |
| CC-LM | Class-Conditional Language Model | 类条件语言模型（受控制码 c 控制） |
| CTRL | Conditional Transformer LM | 控制前缀引导文本生成（Keskar 2019） |
| GeDi | Generative Discriminator Guided seq. | 用生成式判别器在解码阶段引导（Krause 2020） |
| DEXPERTS | Decoding-time Experts / Anti-experts | 解码阶段 expert + anti-expert 组合（Liu 2021） |
| PPLM | Plug-and-Play LM | 用判别器梯度更新隐状态引导（Dathathri 2020） |
| SOC | System on Chip | 片上系统 |
| IC | Integrated Circuit | 集成电路 |
| NLL | Negative Log-Likelihood | 负对数似然（训练目标） |
| V&V | Verification & Validation | 验证与确认 |
| ESL | Electronic System Level | 电子系统级（高于 RTL 的抽象层） |

---

## 九、芯片流程关系

### 9.1 BetterV 在芯片设计流程中的位置

```
 ┌───────────────────────────────────────────────────────────────────────────────┐
 │  系统规格 → 算法/架构 → 微架构 → [ ★ RTL/Verilog 设计 ★ ] → 功能仿真          │
 │                                              │                                │
 │                                              ▼                                │
 │                                     ┌────────────────────┐                    │
 │                                     │  逻辑综合 → 网表    │  ← BetterV 通过 AIG│
 │                                     └────────────────────┘    节点优化直接介入 │
 │                                              │                                │
 │                                              ▼                                │
 │                  ┌───────────────────────────┴───────────────────────────┐       │
 │                  │                                                     │       │
 │                  ▼                                                     ▼       │
 │     ┌────────────────────────┐                            ┌────────────────────┐ │
 │     │  STA / 形式验证 / 仿真 │   ← BetterV 通过 SAT 时间   │  物理设计           │ │
 │     │                        │     优化介入形式验证        │  Floorplan→Place→  │ │
 │     └────────────────────────┘                             │  CTS→Route→PEX    │ │
 │                                                               └────────────────────┘ │
 │                                                                      │              │
 │                                                                      ▼              │
 │                                                           DRC/LVS/Sign-off            │
 └───────────────────────────────────────────────────────────────────────────────┘
```

### 9.2 下游"工业迭代闭环"被压缩的原理

- 工业生产中"Verilog 实现 → 综合 → 检查 PPA → 物理设计 → 验证 → 改 Verilog" 通常需要多轮人工迭代
- BetterV 在生成阶段就让 Verilog 实现偏向 PPA 友好（综合节点少）+ SAT 时间短
- 即下游任务目标（论文中演示的 AIG 节点与 SAT 时间两类）直接进入 LLM 的解码引导信号，相较传统多轮迭代可减少部分"写—测—改"的循环

（需注意：该压缩目前只在论文评测的 AIG 节点 / SAT 时间两类目标上得到验证，并未覆盖时序、功耗、面积、PPA 完整闭环等更广泛的下游任务。）

### 9.3 与本文相关的同期研究 / 交叉工作

- ChatEDA（He et al. 2023）：把 LLM 作为 EDA 的自主 agent
- ChipNeMo（Liu et al. 2023a）：芯片设计领域适配的 LLM
- Thakur et al. 2023：基准测试 LLM 在 Verilog RTL 代码生成上的能力
- VerilogEval（Liu et al. 2023b）：Verilog 生成功能正确性评测基准
- RTLCoder（Liu et al. 2023c）：用问题—答案对与排序反馈微调 RTL 生成
- RTLLM（Lu et al. 2023）：用 self-planning 等 prompt 增强 Verilog 生成
- V2C / v2c（Mukherjee et al. 2016）：Verilog → C 翻译工具
- ANSI-C benchmarks（Mukherjee et al. 2015）：含 SystemVerilog 断言的形式验证基准

### 9.4 论文未触及的 EDA 阶段

- 与数字后端：布局、布线、CTS、寄生参数、签核
- 与可测试性设计（DFT (Design for Testability)）
- 与签核级 STA / 时序收敛
- 与模拟 / 混合信号设计
- 与高层次综合（HLS (High-Level Synthesis)）
- 与形式属性规约（SVA (SystemVerilog Assertions) / Spec）编写

---

## 十、讨论局限（Section 5.1 及留白处）

1. **闭源 LLM 上的可推广性受限**
   - 论文明确指出闭源模型（如 GPT-4）通常不暴露 token-level 概率，因此 Bayes-rule 加权解码所需的 p_LLM 与 p_θ 无法被原作者直接获取
   - 把 BetterV 的"可控引导 + 判别器强化"思路移植到闭源模型仍是未解决问题

2. **判别器引入的额外推理开销**
   - 即使论文采用 TinyLlama / DeepSeek-Coder-1.3b 这种小模型，每一步解码都要调用一次判别器（含 $c$ 与 $\bar{c}$ 两个分支）
   - 自动机加速（量化、剪枝）应该是降低开销的研究路径
   - 论文没有给出具体的推理延迟 / 吞吐量评测

3. **下游任务种类有限**
   - 论文只演示了"综合后 AIG 节点"与"形式验证 SAT 时间"两个下游目标
   - 其它 EDA 关键目标（时序、功耗、面积、形式属性覆盖率、ATPG (Automatic Test Pattern Generation) 友好度）尚未被引导

4. **数据稀缺问题缓解但未根治**
   - 数据增强以高温度采样补全 module 头 + EDA 过滤为主，仍是"在已有分布内做插值"
   - 对罕见硬件模式（高速 SerDes、自定义总线协议等）效果可能不稳定

5. **超参数对任务敏感**
   - $\lambda$, $w$, $\rho$, $\tau$ 在不同下游任务上分别需要调优
   - 论文没有给出自动调参或对超参鲁棒性的系统分析

6. **与功能正确性的耦合尚有空间**
   - Table 4 显示判别器仅给 CodeLlama pass@1 +2.1、BetterV-CodeLlama-base +0.9 的功能正确性增量
   - 在保证"等价"与"更优"之间，论文采用了"分阶段训练 + 引导阶段偏向属性"，并未给出联合检索 / 约束解码的更强形式

7. **论文未讨论模型规模扩展性**
   - 7B 已是论文最大规模；判别器与 LLM 在 13B+ 规模下的耦合是否仍有效、是否能继续拉开对 GPT-4 的差距，论文未给出实验

8. **跨领域迁移论述停留在"潜力"层面（Section 5.2）**
   - 论及 SQL、LaTeX、XML、基因序列等结构化语言
   - 但文中只有结论与展望，没有给出这些领域的迁移实验

---

## 十一、复现信息

### 11.1 公开资源

- 论文 PDF / 文本：本目录下的 `2402.03375_BetterV.pdf`（610 KB）及对应 TXT 文件
- arXiv 编号：2402.03375v3
- 发表：ICML 2024

### 11.2 实现所需工具与依赖

- V2C 工具：Verilog to C Translator（Mukherjee, Tautschnig, Kroening. TACAS 2016, LNCS 9636）
- 深度学习栈
  - DeepSpeed-Chat（Yao et al. 2023, arXiv:2308.01320）
  - DeepSpeed ZeRO（Rajbhandari et al. 2020）
  - LoRA（Hu et al. 2021, arXiv:2106.09685）
- 优化器与学习率调度
  - Adam（Kingma & Ba 2014, arXiv:1412.6980）
  - Cosine LR Decay（Loshchilov & Hutter 2016, arXiv:1608.03983）

### 11.3 硬件

- 两块 NVIDIA Tesla V100S PCIe 32 GB
- CUDA 11.4

### 11.4 主干与判别器选型

| 角色 | 候选模型 |
| :--- | :--- |
| 生成式 LLM | CodeLlama-7B-Instruct（Roziere et al. 2023, arXiv:2308.12950） |
| 生成式 LLM | DeepSeek-Coder-6.7b-Instruct（Guo et al. 2024, arXiv:2401.14196） |
| 生成式 LLM | CodeQwen1.5-7B-Chat（Bai et al. 2023, arXiv:2309.16609） |
| 生成式判别器 | TinyLlama（Zhang, Zeng, Wang, Lu 2024） |
| 生成式判别器 | DeepSeek-Coder-1.3b-Instruct（Guo et al. 2024） |

### 11.5 关键超参（论文 Section 4.1）

- LLM 微调：epoch 4、LR 9.65e-6、batch size 4
- 判别器训练：epoch 3、LR 9.65e-6、batch size 8
- LoRA dim：128
- Adam：β1=0.9, β2=0.95
- α 初始化 1，b 初始化 0
- 任务敏感超参：$\lambda$, $w$, $\rho$, $\tau$ 各下游任务分别调整

### 11.6 命令行工具与流程

- VerilogEval 仿真（pass@k，n=20，k ∈ {1, 5, 10}）
- 综合 + AIG 节点统计：Yosys `proc; aigmap; stat`
- 形式验证 + SAT 求解：Yosys `hierarchy; proc; opt; sat -verify -seq 100 -tempinduct -prove-asserts`
- 语法检查：Yosys `prep`
- 等价性检查：Yosys `eqy` 工具

### 11.7 评测基准

- 功能正确性：VerilogEval（machine / human 两个子集，Liu et al. 2023b）
- SAT 时间：ANSI-C Benchmark（Mukherjee et al. 2015）

### 11.8 复现建议（按论文未明列的部分）

- 数据增强的高温度采样温度未在论文中显式给出；可参照同期代码生成工作常用 0.7—1.0 范围
- 词表对齐是判别器 Plug-and-Play 的必要条件；切换 LLM 时建议核查 tokenizer
- λ, w, ρ, τ 的取值需在各自下游任务上做网格搜索或贝叶斯优化
- CodeQwen1.5-7B-Chat 没有可用的更小版本，因此无法与判别器组合，只参与功能正确性对比

### 11.9 引用要点（实现时常需要复用的对照文献）

- VerilogEval：Liu et al. 2023b（ICCAD 2023, pp. 1-8, "VerilogEval: Evaluating Large Language Models for Verilog Code Generation"）
- RTLCoder：Liu et al. 2023c（arXiv:2312.08617）
- DeepSeek-Coder：Guo et al. 2024（arXiv:2401.14196）
- CodeLlama：Roziere et al. 2023（arXiv:2308.12950）
- Qwen：Bai et al. 2023（arXiv:2309.16609）
- GeDi：Krause et al. 2020（arXiv:2009.06367）
- CTRL：Keskar et al. 2019（arXiv:1909.05858）
- DEXPERTS：Liu et al. 2021（arXiv:2105.03023）
- PPLM：Dathathri et al. 2020（ICLR 2020）
- CoOp discriminators：Holtzman et al. 2018（arXiv:1805.06087）
- V2C：Mukherjee, Tautschnig, Kroening 2016（TACAS, LNCS 9636, pp. 580-586）
- ANSI-C benchmarks：Mukherjee, Kroening, Melham 2015（IEEE ISVLSI 2015, pp. 7-12）
- ChipNeMo：Liu et al. 2023a（arXiv:2311.00176）
- ChatEDA：He et al. 2023（MLCAD 2023, pp. 1-6）

---

## 十二、一分钟复述

BetterV 把 Verilog 生成从一个"写出可编译代码"的任务，重定义为一个"在 Verilog 一开始就把 EDA 下游目标同时考虑进去"的优化任务。它从 GitHub 抓取开源 Verilog，经过定义—Body 拆分和 V2C Verilog→C 对齐，做成 (Verilog, C) 与 (Definition, Body) 两种指令—答案对；用 LoRA 把 6.7B/7B 代码大模型（CodeLlama / DeepSeek-Coder / CodeQwen）在这套数据上 instruct-tuning，并在高温度采样 + EDA 工具语法过滤下不断扩充数据集。

判别器是一个二元生成式 CC-LM，用 EDA 工具（AIG 节点数 / Yosys `eqy` / Yosys `prep` / SAT 时间）给 desired / undesired 两种属性打标；它的训练目标由生成损失 + 判别损失组合而成（$\lambda$ 平衡），解码时通过 Bayes rule 把 LLM 的 token 概率和判别器的类概率相乘，再做一次"累积概率 $\geq \rho$ + 判别置信度 $> \tau$"的 token 过滤。

在 VerilogEval 上，BetterV-CodeQwen 把 pass@1 拉到 68.1（machine）/ 46.1（human），比 GPT-4 高 8.1 / 2.6；在综合后 AIG 节点上和参考 Verilog 相比平均少 46.52%，比自己的 base 版本少 31.68%；在 ANSI-C Benchmark 上把 SAT 求解时间相对参考省 22.45%。BetterV 把"判别器引导的可控文本生成"从控制语言风格带到了控制部分 EDA 工程指标（综合后 AIG 节点 / SAT 时间），在该范围内可减少多轮"写 Verilog—综合—改写"的迭代；代价是对词表一致性的依赖、对闭源模型难移植、以及对下游任务敏感的若干超参数。

(整篇讲解 700+ 行，所有事实均取自论文正文与图表，未做虚构。)
